/**
 * Clean graph-paper background (user reference image 1).
 * Soft off-white field + faint coral grid lines.
 */
export function Atmosphere() {
  return (
    <div className="atmosphere" aria-hidden>
      <div className="atmosphere-paper" />
      <div className="atmosphere-grid" />
    </div>
  );
}
