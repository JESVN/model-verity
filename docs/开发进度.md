# 开发进度

> 更新约定：每完成一个可交付切片就改本文件。  
> UI 模块必须带：`UI: Not started | In preview | Approved | Logic wired`

---

## 总览

| Phase | 名称 | 状态 |
|-------|------|------|
| 0 | 专属目录 + 文档骨架 | **Done** |
| 1 | Design tokens + 纯 `ui/` 组件 | **Done · UI: Approved** |
| 2 | Preview Lab 全场景 + 人工验收 | **Done · Review #001/#002 Approved** |
| 3 | CLI + 本地 server 空壳 | **Done** |
| 4 | `core` 协议引擎 | **Done** |
| 5 | Adapters + SQLite + K4 密钥 | **Done · keychain 环境降级见《待处理事项》OI-003** |
| 6 | 真数据接入已批 UI | **Done · UI: Logic wired** |
| 7 | History / 设置 / 导出收尾 | **Done · npm 0.1.0 Published** |
| 8 | 安全删除 / 后台恢复 / 连接测试 / 结果证据增强 | **Done** |
| 9 | 综合服务声明验证、生产切换和全新数据状态 | **Done · Production deployed · Review #006 Approved** |
| 10 | 自建参考样本采集、质量门禁和不可变版本发布 | **Done · Production deployed · Review #006 Approved** |
| 11 | 全站术语解释、自动推荐和 P1/P2 身份条件硬门禁 | **Done · Production deployed · Review #006 Approved** |
| 12 | 当前验证与参考采集的导航按钮实时进度 | **Done · Production deployed · Review #006 Approved** |
| 13 | V3 评分制、验证页简化和结果页简化 | **Done · Production deployed · Review #006 Approved** |
| 14 | 全站小号文字可读性与普通语言解释 | **Done · Production deployed · Review #006 Approved** |
| 15 | 文案去重与全局文字颜色对比度 | **Done · Production deployed · Review #006 Approved** |
| 16 | 验证默认选择、浏览器偏好与全局加载状态 | **Done · Production deployed · Review #006 Approved** |
| 17 | 供应商编辑独立弹窗 | **Done · Production deployed · Review #006 Approved** |
| 18 | 协议/地址变更沿用已保存 API Key | **Done · Production deployed · Review #006 Approved** |
| 19 | 编辑弹窗保存成功自动关闭 | **Done · Production deployed · Review #006 Approved** |
| 20 | 参考表单模型列表获取与产品形态说明 | **Done · Production deployed · Review #006 Approved** |
| 21 | 供应商长元数据操作区布局修复 | **Done · Production deployed · Review #006 Approved** |
| 22 | 自建参考表单语义去重与分步确认 | **Done · Production deployed · Superseded by Review #009 Approved** |
| 23 | 参考样本信息架构与版本治理布局 | **Done · Production deployed · Superseded by Review #009 Approved** |
| 24 | 参考状态颜色、选中反馈、即时切换与来源筛选 | **Done · Production deployed · Review #009 Approved** |
| 25 | 首次加载与保存/加载性能修复（scrypt 缓存 + bootstrap 单请求） | **Done · Production deployed · Review #010 Approved** |

---

## Phase 0 — 文档与仓库骨架

| 项 | 状态 |
|----|------|
| `/root/projects/model-verity` 创建 | ✅ |
| docs 全套 | ✅ |
| `package.json` / tsconfig / vite | ✅ |
| README | ✅ |

---

## Phase 1 — UI 组件

| 组件 / 模块 | UI 状态 | 备注 |
|-------------|---------|------|
| Tokens / theme | **Approved** | 固定深色主题；删除浅色/System 模式 |
| App shell（顶栏+侧栏） | **Approved** | 深色栏位、暖橙选中态 |
| Background | **Approved** | 48px 淡暖橙方格；无粒子、光球和线稿装饰 |
| Setup 表单 | **Approved · Review #002** | 项目级深色 Select；主按钮为静态暖橙常规样式 |
| Theme Select | **Approved · Review #002** | 无原生 `<select>/<option>`；完整键盘与 ARIA 支持 |
| Running 进度卡 + ProgressRing | **Approved** | |
| Result 英雄区 + 三色 | **Approved** | `likely_match` / `inconclusive` / `likely_mismatch` / `failed` |
| Evidence 展开 | **Approved** | P09 |
| Providers / References / History 列表 | **Approved** | 侧栏可切 |
| Reduced motion | **Approved** | 大位移动效降级；主按钮本身无循环动画 |

---

## Phase 2 — Preview Lab

| 项 | 状态 |
|----|------|
| `npm run preview:ui` | ✅ `http://127.0.0.1:5173/?preview=1` |
| Fixtures P01–P12 | ✅ |
| `npm run typecheck` | ✅ |
| `npm run build:ui` | ✅ → `dist/web` |
| Review #001 | ✅ **Approved · 2026-07-23** · 初始主画布 |
| Review #002 | ✅ **Approved · 2026-07-24** · 主题化 Select + 常规主按钮 |
| 当前 UI 基线 | ✅ `verification-ui@v1.2` |
| UI 业务接线门禁 | ✅ 已解除 |

---

## Phase 3 — CLI + 本地 server

| 项 | 状态 |
|----|------|
| `start --host/--port` 前台服务 | ✅ |
| `stop` / `status --json` / `open` | ✅ |
| PID/runtime 状态与陈旧状态清理 | ✅ |
| SPA 静态托管与 client route fallback | ✅ |
| `GET /api/health` | ✅ |
| SIGINT/SIGTERM 清理 | ✅ |
| Node 构建与可执行 CLI | ✅ |

## Phase 4 — Core 协议引擎

| 项 | 状态 |
|----|------|
| 双 10×4 = 40 cells 版本化 battery | ✅ `project-equivalent@v1` + 精确 `pamela@1.0.0` |
| 协议专用多语言归一化 | ✅ normalize v1 + PAMELA MIT 归一化移植 |
| 经验分布与 base-2 JSD | ✅ |
| 指纹聚合与 cell 排除 | ✅ |
| 四档 verdict 映射 | ✅ |
| Quick/Audit/Full/Enroll seeded planner | ✅ |
| 并发、重试、Retry-After、取消与熔断 | ✅ |
| Core 单测与 mock 指纹端到端 | ✅ 含 PAMELA 精确协议与 planner 检查 |

## Phase 5 — Adapters、SQLite、密钥与 API

| 项 | 状态 |
|----|------|
| Adapter 通用接口/registry | ✅ |
| OpenAI-compatible adapter | ✅ |
| Anthropic Messages adapter | ✅ |
| SQLite schema、WAL、repository | ✅ |
| Keychain 优先动态加载 | ⚠️ 当前主机不可加载，见 OI-003 |
| AES-256-GCM 加密文件回退 | ✅ 0600/0700 |
| Providers/References/Runs/Settings API | ✅ 历史兼容 API；当前前端不调用 Settings |
| Enrollment/Audit 后台 RunManager | ✅ 按参考自动选择 battery/system/normalizer |
| 内置研究参考目录 | ✅ 33 主流模型，只读 gzip，DOI/许可/来源标注 |
| HTTP/API/adapter/storage 测试 | ✅ 17 tests（全套合计）+ API smoke |

## Phase 6 — 真实业务 UI 接线

| 项 | 状态 |
|----|------|
| 默认入口切换为真实 App | ✅ Preview 保留 `?preview=1` |
| Providers 添加/编辑/删除 | ✅ Key 仅掩码 |
| Enrollment 页面与进度 | ✅ |
| Audit setup 与 claimed identity | ✅ 内置精确模型名自动匹配参考 |
| 轮询真实进度与取消 | ✅ |
| Result/JSD/reliability/weak model | ✅ |
| HTTP mock Enrollment→Audit 端到端 | ✅ 8×30 + 8×15 |
| UI 状态 | **Logic wired**；移动/来源展示变更进入 Review #003 |

## Phase 7 — v1 收尾

| 项 | 状态 |
|----|------|
| History 真实运行记录与详情 | ✅ |
| Settings 并发/阈值/raw 开关 | ✅ 历史实现；当前 UI 已移除，服务端仅保留兼容结构 |
| Markdown/JSON/CSV 导出 | ✅ |
| 全量 typecheck/test/build | ✅ |
| `npm pack` 内容检查 | ✅ |
| 临时 prefix 全局安装 smoke | ✅ |
| 正式 npm publish | ✅ `model-verity@0.1.0` |
| 公网域名生产反代 | ✅ systemd + NPM + HTTPS + Basic Auth；2026-08-03 用户确认认证已自动恢复 |

## 对抗式检查

- 状态：✅ 完成
- 范围：阶段完整性、CLI/server、core、adapters、SQLite/密钥、API/同源安全、端到端、构建/打包/依赖审计、远程域名
- 初始结果：发现并修复 30 项缺陷或边界问题；完整记录见 `docs/对抗式检查.md`
- 2026-07-24 追加检查：内置库协议隔离、来源许可、可复现构建、虚拟 reference SQLite 兼容、移动端历史/Select/溢出、参考搜索/分页、供应商角色 eligibility
- 2026-07-25 追加验收：修复 iOS Chrome Basic Auth 连续 401；用户在真实移动设备确认登录成功
- 2026-07-25 追加交互：桌面仅主内容滚动、结果/历史/Enrollment 返回可逆、验证结果可回到验证表单
- 2026-07-25 追加性能：主导航 keep-alive + 轻量入场动画，修复切换卡顿
- 2026-07-27 追加功能：历史删除（单条/批量/全选 + confirm）
- 2026-07-27 审计评分修复：参考 cell 预筛选、自适应 minValid、细化 inconclusive 文案、classifyVerdictDetailed 输出
- 2026-07-27 Review #005 候选：四类安全删除、Audit/Enrollment 持久跟踪与导航状态、连接测试临时会话、结构化结果证据、维护锁和回滚部署脚本
- 2026-07-27 OpenAI Responses：新增独立 `openai-responses` adapter、供应商协议选项、Responses 可见文本/usage/reasoning 解析及连接测试 E2E
- 2026-07-27 9router `cx/gpt-5.5` 诊断：确认请求质量正常但相对 OpenRouter/OpenAI `gpt-5.5-20260423` serving 快照偏移；发现 405/432 vs 40–56 input-token 可比性信号，并用随包 split-half 数据确认 `0.12/0.22` 全局阈值存在高误拒绝、缺少 profile/subset/cross-provider 校准
- 2026-07-28 v2 需求冻结：Grill-me 1–50 完成，选择完整框架一次替换；冻结多维结论、双轨参考、分层身份/协议、校准证据、第一方序贯配对、独立 v2 schema、预算安全和 Review #006 门禁。正式实施计划见 `docs/v2实施计划.md`
- 2026-07-28 v2 离线候选：完成 M0 网页入口暂停、独立 v2 schema、版本化 challenge manifest、calibration artifact 1% 门禁、bootstrap/conformal evidence、五维结论、screening/paired runner、结构化 observation/attempt、预算上限、v2 API、候选 UI 和 JSON 时点报告；没有匹配 calibration 时强制 `uncalibrated`
- 2026-07-28 M8 首批真实矩阵：按用户指定将 9router `cx/gpt-5.5` / `cx/gpt-5.6-sol` 作为 L2 可信参考，对随想和 AiCodeWith 执行 3 场景 × 2 短时窗口 × 20 pairs，共 240 endpoint observations；240/240 HTTP 层成功，所有 run 正确保持 `uncalibrated`。报告见 `docs/M8-9router参考真实验收-20260728.md`
- 2026-07-28 M8 低预算矩阵：执行 880 次真实请求（9router 640、随想 160、AiCodeWith 80），完成随机交错 block、9router split-half、3 个行为阴性和独立 cell holdout。发现 GPT-5.5 与 GPT-5.4 不可可靠区分；AiCodeWith holdout 暴露 token budget fail-closed 缺陷并作废。报告见 `docs/M8-低预算正式矩阵-20260728.md`
- 2026-07-28 协议消歧：同一 9router `cx/gpt-5.6-sol` 执行 Chat/Responses crossover 40 请求，跨协议 JSD 0.058；再以 Responses↔Responses 重跑 AiCodeWith holdout 80 请求，40/40 pairs 完整、JSD 0.342。协议统一后偏移仍在，说明不能仅归因于 schema；runner 支持每端固化实际协议，API 禁止不同协议冒充 P1。报告见 `docs/M8-协议消歧与下一步-20260728.md`
- 2026-07-28 M9.1–M9.10：实现 exploratory-only 硬策略、SQLite 原子请求/token 预算、临时内存凭据、每端协议固化、v2/Legacy History 分层、v2 删除、reference governance repository、7 天 share report、Legacy 410 开关、安全 header/base URL 门禁和生产 DB 副本迁移演练。详情见 `docs/M9工程收尾-20260728.md`
- M9 续作：补齐 v2 Markdown/CSV、报告 endpoint revision/credential scope HMAC、配置变更检测、reference governance API/UI、v2 History 详情/删除/刷新与多标签跟踪、正式 pair 并发启动与 startGap、两端协议 UI、临时 Key UI、DNS/私网 SSRF policy；独立候选已运行于 `http://127.0.0.1:18787/`
- 最新自动验证：39 tests、typecheck、production build、v2 candidate smoke、生产 DB 副本迁移、npm pack dry-run、官方 registry npm audit 0 vulnerabilities
- 2026-07-28 19:34 CST：按用户明确批准部署新验证框架到生产；确定性强结论仍由服务端证据门禁限制，长期稳定性未评估，旧写 API 410。备份：`/root/backups/model-verity/deploy-20260728T113430Z-3286657`；npm 未发布
- 2026-07-28 20:08 CST：按用户要求执行全新版本数据清理。保留 3 个供应商配置及其 3 个加密密钥，删除 8 条历史、2 个本地参考及所有验证/参考/校准/预算/分享/设置数据。清理备份：`/root/backups/model-verity/fresh-start-20260728T120854Z-3297349`
- 生产 UI 已移除内部版本、探索模式、Legacy、暂停和 Enrollment 迁移文案；当前资源 `index-C_DpW4FZ.js` / `index-WI1vRg2h.css`
- 当前文档已同步：`README.md`、`项目计划.md`、`验证协议.md`、`界面规范.md`、`界面验收.md`、`界面验收-Review-006.md`、`后续路线图.md`、`部署指南.md`、`M9工程收尾-20260728.md`、`已完成事项.md`、`待处理事项.md`；新增 `生产数据重置-20260728.md`
- 2026-07-28：移除无可操作选项的“设置”导航和静态页面；数据与隐私说明继续保留在供应商密钥、临时第一方 Key、验证和历史等相关工作流附近。已部署生产，备份：`/root/backups/model-verity/deploy-20260728T144312Z-3341592`

## Phase 10 — 自建参考样本

- 2026-07-29：修复旧 Enrollment 停用后缺少替代流程的功能迁移漏项。
- 新增 `reference_enrollment` 运行类型和 `/api/v2/references/enrollment-plans`。
- 用户选择可信供应商、模型、协议、身份、L2/L3 和固定预算。
- 质量门禁通过后原子发布 reference cohort + immutable version；失败/取消不发布。
- 自建版本自动接入参考样本比对，治理状态控制是否可用。
- 直接 reference version 写入返回 410，防止绕过真实采集和质量门禁。
- TypeScript、40 tests、production build、candidate smoke 和 dependency audit 通过；自动测试未调用真实供应商。
- 已部署生产：`index-BJXpKaC9.js` / `index-WI1vRg2h.css`；备份 `/root/backups/model-verity/deploy-20260728T164908Z-3378535`。

## Phase 11 — 全站术语解释与可比性

- 关键字段统一回答“是什么、怎么选、影响什么”，Select 支持副说明。
- 产品面和 P1/P2/P3 改为中文用户语言，并基于目标/参考元数据给出推荐。
- 实时配对增加参考端厂商、产品、产品面，避免只检查协议的伪 P1。
- 服务端 P1/P2 校验厂商、产品和非 unknown 产品面；P1 额外校验协议。
- 结果、供应商、参考治理、History 和错误提示清理内部英文状态码。
- TypeScript、41 tests、production build、candidate smoke 和 dependency audit 通过；未调用真实供应商。
- 已部署生产：`index-u_4omHRb.js` / `index-0H3_uVLj.css`；备份 `/root/backups/model-verity/deploy-20260728T172851Z-3390666`。

## Phase 12 — 导航按钮实时进度

- 根因：侧栏进度仍读取旧 `ApiRun` track，当前 `ApiV2Run` 只在工作区内部轮询。
- 当前验证运行向 `AppShell` 同步；跨页面时“验证”按钮显示实时百分比。
- 自建参考采集向 `AppShell` 同步；跨页面时“参考样本”按钮显示实时百分比。
- 刷新和多标签恢复会继续同步运行对象与轮询，不只恢复工作区画面。
- TypeScript、41 tests、production build、candidate smoke 和 dependency audit 通过；部署未调用真实供应商。
- 已部署生产：`index-BcoIZfFI.js` / `index-0H3_uVLj.css`；备份 `/root/backups/model-verity/deploy-20260728T182352Z-3406590`。

## Phase 13 — V3 评分制与简化界面

- 完成 VeriDrop 源码静态审计和在线信息层级研究；只吸收 weighted score、critical cap、主动/被动复用和技术详情折叠等抽象原则。
- PAMELA/Zenodo 保持核心，行为一致性占 55%；没有 JSD 直接无法评分。
- 综合评分维度：行为 55%、请求质量 15%、短时稳定性 10%、可比性 10%、参考强度 10%。
- 数值分和结论 cap 分离，避免 P3、旧 L3、低覆盖或行为混合被平均分掩盖。
- 默认验证表单降至五个核心决策；实时配对和身份/协议字段移入高级验证方式。
- 结果页改为评分、等级、关键说明和评分组成；统计细节默认折叠。
- 无 V3 scorecard 的旧完成运行不进入当前 API/UI；用户要求清理旧业务数据。
- TypeScript、47 tests、production build、candidate smoke 和 dependency audit 通过；未调用真实供应商。
- 已部署生产：`index-BIYnOqH2.js` / `index-C37aSYsE.css`；备份 `/root/backups/model-verity/deploy-20260728T192034Z-3424603`。
- 清理前完整备份：`/root/backups/model-verity/v3-fresh-start-20260728T201506Z-3440092`；供应商和加密密钥保留。
- 生产旧验证、观察、attempt、证据、结论、预算、分享、校准、旧参考和设置数据已清空；History/API 当前为空。
- 前端停止读取 `/api/runs`；旧详情、History 合并和 scorecard 派生路径删除。
- 精简部署资源：`index-CVufyNW5.js` / `index-C37aSYsE.css`；备份 `/root/backups/model-verity/deploy-20260728T202031Z-3442069`。
- 2026-07-30 修复 GPT-5.5 内置参考可比性：内置比较恢复固定 PAMELA prompt，实时配对/自建参考保持 marked prompt；manifest 升级 `pamela-challenge@2` 并保存 `promptMode`。
- 内置参考时效按 0–14/15–45/>45 天动态计算；旧 76.7/79.6 分运行不重算。manifest/结果/技术详情显式显示 `promptMode`。当前资源 `index-BOx-PE_M.js` / `index-UKCJhZJ7.css`，部署备份 `/root/backups/model-verity/deploy-20260729T182845Z-3816014`。
- 2026-07-31 已实现并部署 `pamela-scorecard@3.1.0`：将高行为相似度与 P3/研究参考来源限制分开展示；范围受限时以行为分为主数字、综合证据分为次级审计值；权重、JSD 映射、阈值和 cap 不变。资源 `index-BulRbg95.js` / `index-P7SjsYD4.css`，备份 `/root/backups/model-verity/deploy-20260730T175818Z-17614`。
- 已修复并部署评分环仍读取 cap 后 band 的颜色错误：scope-only 结果按主展示分使用 85–100 绿色、70–84 柔和绿、50–69 黄色、0–49 红色、无分灰色；实质质量/稳定性 cap 仍按 cap 后等级着色。55 tests、typecheck、build、smoke、audit 通过。资源 `index-BzaIOLYr.js` / `index-m3t5jrG9.css`，备份 `/root/backups/model-verity/deploy-20260730T184222Z-30584`。

## Phase 14 — 文字可读性与普通语言解释

- 历史记录摘要提升至 13px、1.65 行高和中等字重；列表卡片间距同步增加，保持原深色暖橙风格。
- 全局辅助文字提高对比度：次要文字在主要深色表面约 6.07:1–7.82:1，更弱层级在主要深色表面约 4.65:1–6.00:1。
- 字段说明、Select 副说明、预算提示、结果关键说明和技术详情统一提高字号、行高与字重；11–12px 仅保留给短 badge、状态和紧凑技术元数据。
- 协议、产品形态、P1/P2/P3、L1/L2/L3、模型 ID、参考样本、预算、token、时效、质量、五维结果、JSD、校准、重复抽样区间和采样清单均改为普通语言解释，并保留必要专业代码供报告核对。
- TypeScript、57 tests、production build 和 candidate smoke 通过；自动检查未调用真实供应商。
- 已部署生产：`index-CcmWJCWQ.js` / `index-Dvad-M3x.css`；备份 `/root/backups/model-verity/deploy-20260731T133620Z-358330`。
- 部署后 systemd、上游/公网 health/status、API policy、供应商读取、SQLite integrity、维护锁和候选临时服务清理均通过；相关桌面、移动、键盘、长文案和技术详情检查已纳入 2026-08-03 Approved 的 Review #006。

## Phase 15 — 文案去重与颜色对比度

- 选项说明总字数从 1111 降至 736（约 -34%），字段帮助从 580 降至 278（约 -52%），页面导语从 259 降至 147（约 -43%）；证据边界、选择条件和费用影响仍保留。
- 评分组成只显示每项含义，不再重复技术数值说明；原始数值和统计解释继续留在折叠技术详情。
- 次要文字在最不利主要表面上的对比度由 6.07:1 提升至 7.59:1，弱辅助文字由 4.65:1 提升至 5.67:1。
- 成功、警告、危险和低分状态统一使用语义颜色 token，最低对比度为 4.96:1；移除活跃界面的零散硬编码文字色。
- 暖橙主按钮改用深色文字，default/hover 对比度至少 5.78:1；按下态使用深橙底白字。
- TypeScript、57 tests 和 production UI build 通过；未调用真实供应商。
- 已部署生产：`index-BTXI50li.js` / `index-CFVSbpSn.css`；备份 `/root/backups/model-verity/deploy-20260731T145948Z-382372`。
- 部署后 systemd、上游/公网 health/status、API policy、供应商读取、SQLite integrity、维护锁、候选服务清理和公网新文案检查均通过；相关桌面、移动、键盘和扫读效率检查已纳入 2026-08-03 Approved 的 Review #006。

## Phase 16 — 默认选择、浏览器偏好与全局加载状态

- 根数据加载完成后再挂载验证表单，避免空数据首帧覆盖默认值或产生选项闪烁。
- 新浏览器默认选择第一个验证供应商、该供应商第一个模型和第一个参考样本；已保存选择有效时优先恢复，无效时回退到当前第一个可用项。
- 验证方式、供应商/模型、参考、检测深度、协议和身份字段写入当前浏览器偏好；临时 API Key、预算授权、credential session 和运行状态不写入。
- History“再次验证”继续独立恢复历史配置，资源缺失时 fail-closed，不被浏览器偏好静默替换。
- 新增全局 `LoadingState` 暖橙四格动画，覆盖根数据、参考治理数据和上次运行恢复；包含 ARIA 状态，reduced-motion 下停止循环动画。
- TypeScript、60 tests、production build、candidate smoke 和依赖审计通过；未调用真实供应商。
- 已部署生产：`index-DtKLjZlO.js` / `index-XjIFl9Df.css`；备份 `/root/backups/model-verity/deploy-20260731T163129Z-409144`。
- 部署后 systemd、上游/公网 health/status、API policy、3 个供应商、146 个参考、SQLite integrity、维护锁、候选服务清理及公网偏好/加载资源检查均通过。
- 首次默认、刷新恢复、资源失效、History 隔离、移动端和 reduced-motion 检查已纳入 2026-08-03 Approved 的 Review #006。

## Phase 17 — 供应商编辑独立弹窗

- 供应商页“编辑”不再复用“添加供应商”表单，改为独立对话框，避免在添加区域直接改写已有供应商造成误解。
- 编辑弹窗包含名称、Base URL、API Key（留空保留原密钥）、协议、角色、模型 ID 和模型列表获取；保存成功后关闭并刷新列表。
- 取消、弹窗外点击关闭和保存中禁用沿用现有 dialog 交互；错误原位显示在弹窗内。
- 弹窗复用管理表单双列网格，窄屏折叠单列；后端 `POST /api/providers` 语义不变。
- TypeScript、60 tests、production build、candidate smoke 和 dependency audit 通过；未调用真实供应商。
- 已部署生产：`index-CL7QJ7uG.js` / `index-BKiiD5YA.css`；备份 `/root/backups/model-verity/deploy-20260731T165550Z-416834`。
- 部署后 systemd active，health/status、API policy、供应商读取和 SQLite integrity 检查通过，维护锁已清除。
- 编辑弹窗桌面、移动、键盘和焦点检查已纳入 2026-08-03 Approved 的 Review #006。

## Phase 18 — 协议/地址变更沿用已保存 API Key

- 移除“协议或 Base URL 变化必须重输 API Key”的 400 限制：保存供应商和获取模型列表时，未填 Key 直接沿用已保存加密密钥。
- 协议与地址只描述请求路径和字段，API Key 可跨协议复用；只有首次新增或确实换钥才需要填写。
- 获取模型列表对未保存的供应商仍要求提供 Key。
- e2e 改为验证变更协议/地址后仍用已保存 Key 成功获取模型与保存供应商，并恢复原目标继续后续流程。
- TypeScript、60 tests、production build、candidate smoke 和 dependency audit 通过；未调用真实供应商。
- 已部署生产：`index-CL7QJ7uG.js` / `index-BKiiD5YA.css`；备份 `/root/backups/model-verity/deploy-20260731T172437Z-426378`。
- 部署后 systemd active，health/status、API policy、供应商读取和 SQLite integrity 检查通过，维护锁已清除。
- 编辑弹窗与协议变更的桌面、移动和键盘检查已纳入 2026-08-03 Approved 的 Review #006。

## Phase 19 — 编辑弹窗保存成功自动关闭

- 编辑弹窗“保存修改”成功后自动关闭并刷新列表；保存失败保持弹窗打开，错误原位显示。
- 添加供应商表单行为不变（保存后清空表单并显示提示）。
- TypeScript、60 tests、production build、candidate smoke 和 dependency audit 通过；未调用真实供应商。
- 已部署生产：`index-CH0bGsuj.js` / `index-BKiiD5YA.css`；备份 `/root/backups/model-verity/deploy-20260731T172855Z-428386`。
- 部署后 systemd active，health/status、API policy、供应商读取和 SQLite integrity 检查通过，维护锁已清除。
- 编辑弹窗桌面、移动和键盘检查已纳入 2026-08-03 Approved 的 Review #006。

## Phase 20 — 参考表单模型列表获取与产品形态说明

- 创建参考样本表单新增“从模型列表接口获取”选项：从所选可信供应商读取服务声明的模型 ID，选择后填入参考端模型；复用已保存密钥，不修改供应商配置。
- 获取结果明确“列表只表示服务声称支持这些 ID，不证明模型身份”；已配置的模型在选项中标注。
- 重写“产品形态”五个选项说明：解释它指模型以哪种方式被提供和使用（API、网页产品、编程产品、企业部署），同一模型在不同形态下回答可能不同，比较时目标与参考应选同一形态。
- 字段名改为“产品形态（使用方式）”，帮助文案补充选择影响；选项说明保持 20–55 字简洁约束。
- TypeScript、60 tests、production build、candidate smoke 和 dependency audit 通过；未调用真实供应商。
- 已部署生产：`index-Dxk-rsiN.js` / `index-BKiiD5YA.css`；备份 `/root/backups/model-verity/deploy-20260731T174016Z-432647`。
- 部署后 systemd active，health/status、API policy、供应商读取和 SQLite integrity 检查通过，公网/安装目录/本地资源 hash 一致，维护锁已清除。
- 参考表单与产品形态说明的桌面、移动和键盘检查已纳入 2026-08-03 Approved 的 Review #006。

## Phase 21 — 供应商长元数据操作区布局修复

- 根因是较长地址和多个模型增大列表正文的 Flex 基准宽度，操作区仍允许收缩，中文“测试连接”和“编辑”因此逐字换行。
- 供应商操作区改为不收缩，文字按钮保持单行；正文继续安全断行，600px 以下操作组继续按现有移动布局换行。
- 新增长元数据布局回归测试；TypeScript、61 tests、production build、candidate smoke 和 dependency audit 通过；未调用真实供应商。
- 已部署生产：`index-BpfOVBah.js` / `index-HOLV30RT.css`；备份 `/root/backups/model-verity/deploy-20260802T165832Z-1224118`。
- 部署后 systemd active，上游 health/status、API policy、3 个供应商、146 个参考、SQLite integrity/quick check、资源 hash 和维护锁检查通过。
- 用户确认 Basic Auth 已自动恢复；公网未认证 HTTPS 返回 401，HTTP 保持 301。候选、Vite 和 smoke 临时服务已清理。
- 2026-08-03 用户确认生产验证正常，Review #006 Approved。

## Phase 22 — 自建参考表单语义去重

- 将平铺表单重组为“从哪里采集 / 这份参考代表什么 / 选择采集规模”三个步骤。
- 明确区分发送给接口的模型 ID 与用户声明的实际上游模型；默认联动，网关别名按需展开，人工修改后不被模型选择覆盖。
- 模型列表获取并入模型 ID 控件；未配置远程模型不可绕过供应商 allowlist 启动。
- 参考名称改为自动生成；自定义名称、协议覆盖和临时 Key 收入高级设置。
- 移除固定 OpenAI 默认声明；切换供应商清除旧上游厂商；启动前显示请求路径和身份摘要。
- 不改变 schema、评分、预算、0 重试、质量门禁、manifest 或证据等级。
- TypeScript、65 tests、production build、candidate smoke 和 dependency audit 通过；未调用真实供应商。
- 已部署生产：`index-BwfwFXeS.js` / `index-CQB2NMqV.css`；备份 `/root/backups/model-verity/deploy-20260802T184243Z-1254531`。
- 部署后 systemd active；health/status/policy、3 个供应商、146 个参考、SQLite integrity/quick check、资源 hash 和维护锁检查通过。
- 新建 Review #007；自动门禁通过，后续参考样本布局变更由 Review #008 合并承接人工验收。

## Phase 23 — 参考样本信息架构与版本治理布局

- 将自建参考和公开研究样本库拆成两个独立卡片，避免创建、治理、搜索和列表平铺在同一层级。
- 自建参考卡集中展示参考组/版本数量、创建入口、版本选择、当前时效/质量状态和治理操作。
- 研究搜索移入研究参考库卡片，与匹配数量、结果和分页保持同一上下文。
- 720px 以下标题、统计和状态转为单列；600px 以下创建按钮占满宽度，保留 44px 触控目标。
- 不改变 schema、API、评分、预算、质量门禁、0 重试、manifest、证据等级或既有记录。
- TypeScript、66 tests、production build、candidate smoke 和 dependency audit 通过；未调用真实供应商。
- 已部署生产：`index-BqdFqZID.js` / `index-D3DIkwwF.css`；备份 `/root/backups/model-verity/deploy-20260803T154402Z-1605439`。
- 部署后 systemd active；health/status/policy、3 个供应商、146 个研究参考、1 个自建参考、SQLite integrity/quick check、资源 hash 和维护锁检查通过。
- 仅生产 `127.0.0.1:8787` 保持监听；候选、Vite、Preview 和 smoke 临时服务均无遗留。

## Phase 24 — 参考状态颜色、选中反馈与即时切换

- 根因一：治理按钮没有选中态或 `aria-pressed`，用户无法识别当前操作对应的状态。
- 根因二：前端提交后重新加载完整版本列表，且没有锁定并发点击，响应与 reload 可能产生视觉竞态。
- 根因三：“标记为过期”只更新 freshness，保留旧 `review_required/quarantined` quality，导致旧质量状态继续主导当前状态。
- 状态卡增加绿色、柔和绿、黄色、品牌橙和中性灰语义，同时保留圆点、总体文字、时效/质量 badge 和影响说明。
- 点击状态后乐观更新并锁定其他按钮；失败回滚，成功使用治理 API 返回的完整版本校正，不再依赖额外 reload。
- 治理按钮增加单选式对勾、边框、底色和 `aria-pressed`；“标记为过期”规范为 `stale + approved`，仍不可用于新验证。
- TypeScript、67 tests、production build、V2 candidate smoke 和 dependency audit 通过；未调用真实供应商。
- 已部署生产：`index-Bg-mSy5I.js` / `index-D8ErvPNs.css`；备份 `/root/backups/model-verity/deploy-20260803T163556Z-1621098`。
- 部署后 systemd active/running，`NRestarts=0`；health/status/policy、旧写 API 410、SQLite integrity/quick check、资源 hash 和维护锁检查通过。
- 部署未触发治理操作，自建参考保持 `current/approved`；仅生产 `127.0.0.1:8787` 监听，无候选、Vite、Preview 或 smoke 遗留服务。

## 当前焦点

1. 当前生产为 Review #010，已于 2026-08-05 通过用户生产环境人工验收（加载/保存性能）；Review #007/#008/#009 人工项已随前序 Review 关闭。
2. 内置研究参考已缩减为 33 个主流厂商模型并部署生产；参考样本页已上线 Zenodo 在线更新（检查/下载准备/勾选更新/回滚/缓存清理）；当前生产资源 `index-BcvSqdz_.js` / `index-DzrdqyBF.css`。
2. 首次真实参考采集仍需用户在 UI 明确批准供应商、模型和固定预算。
3. 当前生产保持五维证据表达，不启用未经独立数据证明的 1%/1% 确定性强结论。
4. 重置前的旧历史、用户参考、校准、预算和分享数据已清空；当前新增记录按现行 schema 保留。
5. 不追加真实供应商采样，除非用户单独批准模型、请求和 token 预算。
6. npm 新版本需单独批准，不自动发布。

## 环境注意

- 本机 `NODE_ENV=production` 时，`npm install` 会跳过 devDependencies。  
  请用：`npm install --include=dev` 或 `npm run install:dev`。
- Vite Preview 仅在执行 `npm run preview:ui` 时启动；生产环境不运行 5173。生产 CLI 默认监听 `127.0.0.1`，本站 systemd 实例仅监听回环地址 `127.0.0.1:8787`。

## 风险 / 阻塞

| 风险 | 缓解 |
|------|------|
| 已批准 UI 被业务接线顺带改版 | 保持展示组件边界；重大变化新开 UI Review |
| 两类 prompt 被误混用 | 精确 PAMELA 资源已从软件 DOI 取得；以三版本元数据隔离，禁止跨协议比较 |
| 研究快照被误解为官方 | UI/Result/README 强制显示 OpenRouter、DOI、CC BY 4.0 与非厂商认证 |
| 密钥误泄露 | Phase 5 优先 keychain，回退本机加密文件；日志和导出脱敏 |

**当前全新生产界面和数据重置已完成；Review #006 已于 2026-08-03 Approved。npm 新版本仍需单独批准。**
