# V3 生产数据重置审计记录

> 执行日期：2026-07-29（CST）  
> 原因：用户明确要求不兼容旧数据、不显示旧内容，直接删除旧业务数据。  
> 保留范围：供应商配置、供应商加密密钥、服务报告绑定密钥。  
> 操作性质：不可逆；清理前已建立完整可恢复备份。

## 1. 清理前门禁

```text
systemd：active
activeRun：false
activeConnectionTest：null
SQLite integrity：ok
providers：3
verification_runs_v2：2
challenge_observations_v2：100
observation_attempts_v2：100
budget_authorizations_v2：3
```

供应商配置 hash：

```text
cdf48cd8c54df2266b19504996d970d6d0bec5eec99a670196a3be5630c5a275
```

## 2. 完整备份

```text
/root/backups/model-verity/v3-fresh-start-20260728T201506Z-3440092
```

备份包含：

- 完整生产数据目录；
- 完整生产安装目录；
- systemd unit 和 drop-in；
- 清理前各表计数；
- 供应商配置 hash；
- 加密密钥文件 SHA-256。

## 3. 删除范围

以下表已在 SQLite transaction 中清空：

```text
share_reports_v2
verification_conclusions_v2
verification_evidence_v2
observation_attempts_v2
challenge_observations_v2
challenge_blocks_v2
challenge_manifests_v2
verification_run_endpoints_v2
verification_runs_v2
budget_authorizations_v2
calibration_artifacts_v2
calibration_profiles_v2
reference_governance_events_v2
reference_versions_v2
reference_cohorts_v2
provider_model_mappings_v2
model_identities_v2
verification_series_v2
runs
reference_fingerprints
settings
```

清理启用了：

```text
PRAGMA secure_delete=ON
PRAGMA wal_checkpoint(TRUNCATE)
VACUUM
```

## 4. 保留范围验证

清理后：

```text
providers：3
供应商配置 hash：cdf48cd8c54df2266b19504996d970d6d0bec5eec99a670196a3be5630c5a275
secrets.enc.json SHA-256：与清理前一致
master.key SHA-256：与清理前一致
SQLite integrity：ok
```

当前保留供应商：

```text
随想
AiCodeWith
9router
```

## 5. 代码与界面处理

- 前端停止调用 `/api/runs`，只读取当前 `verification_runs`。
- History 不再合并旧运行。
- 无 V3 scorecard 的完成运行不进入当前 API/UI。
- 删除旧运行详情、旧参考采集详情和旧评分派生路径。
- 旧写 API 继续返回 `410 Gone`，仅作为退役安全门禁，不用于兼容展示。
- 浏览器中残留的旧 localStorage track 若指向已删除运行，会在请求 404 后自动清除。

## 6. 自动门禁

```text
TypeScript：通过
tests：45/45
production build：通过
candidate smoke：通过
dependency audit：0 vulnerabilities
```

自动测试、构建、部署和数据清理未调用真实供应商。

## 7. 生产结果

本次数据重置部署资源（历史记录，当前资源见 `docs/界面验收-Review-006.md`）：

```text
assets/index-CVufyNW5.js
assets/index-C37aSYsE.css
```

精简代码部署备份：

```text
/root/backups/model-verity/deploy-20260728T202031Z-3442069
```

最终状态：

```text
systemd：active
maintenance：false
activeRun：false
activeConnectionTest：null
/api/v2/verification-runs：[]
/api/v2/history：[]
业务表：全部 0
providers：3
SQLite integrity：ok
```
