# 未完成、阻塞与待人工验证台账

> 创建于 2026-07-23。Phase 3–7 开发期间持续更新。  
> 规则：单项受阻不停止后续可执行工作；记录尝试、结果与建议。

## 状态定义

- `Open`：尚未解决
- `Mitigated`：已有降级方案，仍需后续处理
- `Needs human verification`：实现已完成，但需要人工或真实环境确认
- `Resolved`：曾受阻，现已解决（保留记录）

## 当前事项

### OI-019 — 首次加载与保存/加载性能修复人工验收

| 字段 | 内容 |
|------|------|
| Phase / 事项 | 性能 / 首次加载与保存/加载路径（scrypt 重复派生 + 代理每请求 bcrypt × 多次往返） |
| 状态 | `Resolved` |
| 阻塞或人工验证原因 | 用户已在生产环境人工验证。 |
| 已尝试方法及结果 | 根因①：`SecretStore` 每次 get/set 重跑内存昂贵 KDF（scrypt ≈58ms/次），`/api/providers` 3 个 provider ≈150ms（其它端点 10 倍）；改为进程级 AES 密钥缓存（按 salt）+ 解密文档缓存，写时失效，密钥/格式/安全语义不变，实测 60 次读取 ~9s→0.3ms，生产 `/api/providers` ~150ms→~2ms。根因②：openresty 整站 `auth_basic` 为 bcrypt `$2a$10$`，nginx 每请求重算不缓存（≈132ms/请求、2 worker），叠加 app 每次动作 5-7 个往返；新增 `GET /api/bootstrap` 一次返回 providers/references/runs/status，`App.refresh()` 改单请求（首次加载 7→3、保存后刷新 4→1）。另：前端 `request()` 加 15s 有界超时防加载页永久停留；顺带移除客户端无调用方死代码。TypeScript、68 tests、build、candidate smoke、audit 0 漏洞通过；已部署生产并 post-check（health/status、bootstrap 字段完整、SQLite integrity ok、维护锁清除）。未调用真实供应商、未发布 npm。 |
| 建议后续操作 | 2026-08-05 用户确认正常，Review #010 Approved。可选项（未做）：治理组件挂载的 cohorts/versions 2 个请求并入 bootstrap；代理层 bcrypt 轮数/缓存属 1panel 托管配置，削弱认证，不建议动。npm 发布仍需单独批准。 |

### OI-018 — UI Review #009 参考状态切换部署与人工验收

| 字段 | 内容 |
|------|------|
| Phase / 事项 | UI / 参考状态颜色、选中反馈与即时切换 |
| 状态 | `Resolved` |
| 阻塞或人工验证原因 | 根因修复、68 tests、typecheck、production build、V2 candidate smoke、audit 和生产后置检查已通过，且用户已确认。 |
| 已尝试方法及结果 | 生产已部署 `index-IJMiDkPo.js` / `index-8VsZv4RY.css`；状态卡增加分级颜色、圆点、总体文字和时效/质量 badge；按钮增加 `aria-pressed` 和选中态；前端乐观更新、锁定并发、失败回滚；API 返回转换后版本；过期操作清除旧质量状态但仍禁止新验证。验证页参考样本增加“全部 / 自建参考 / 研究参考”来源筛选并显示数量。未调用真实供应商。 |
| 建议后续操作 | 2026-08-04 用户在生产环境确认正常，Review #009 Approved；Review #007/#008 UI 人工项一并关闭。npm 发布仍需单独批准。 |

### OI-017 — UI Review #008 参考样本页综合人工验收

| 字段 | 内容 |
|------|------|
| Phase / 事项 | UI / 自建参考表单语义与参考样本信息架构 |
| 状态 | `Superseded by Review #009` |
| 阻塞或人工验证原因 | Review #008 已部署，但其参考状态反馈随后发现颜色和即时切换问题；未完成人工项不能按旧资源单独批准。 |
| 已尝试方法及结果 | Review #008 的分区布局和 Review #007 的表单语义均保留在 Review #009 生产资源中；后续状态根因修复见 `docs/界面验收-Review-009.md`。 |
| 建议后续操作 | 统一按 Review #009 验收，避免对已被替代的资源重复检查。npm 发布仍需单独批准。 |

### OI-015 — 综合服务声明验证统计门禁

| 字段 | 内容 |
|------|------|
| Phase / 事项 | Core stats / paired verification / reference governance |
| 状态 | `Production deployed · Strong statistical claims blocked` |
| 阻塞或人工验证原因 | 当前数据没有证明“行为支持”误接收率和“明显异常”误拒绝率分别达到预注册的 1% 目标。GPT-5.5 genuine 0.229 与 GPT-5.4 negative 0.237 重叠，现有 probes 不能可靠区分。 |
| 已尝试方法及结果 | 已执行 880 次低预算真实矩阵、Chat/Responses crossover、同协议 holdout、内部稳定性和行为阴性；完成原子预算、pair 调度、协议门禁和稳定性检测。2026-07-31 只读复核 86.2 分生产运行，确认 JSD 0.021452、80/80 成功、覆盖 100%、短时稳定，最终 review 仅由 P3/未校准范围 cap 触发；生产 `pamela-scorecard@3.1.0` 已将行为相似度与来源范围拆分，未放宽 cap。详见 M8 报告及 `docs/v3实施计划.md`。 |
| 建议后续操作 | `3.1.0` 双分数与分级色阶已部署，并已纳入 2026-08-03 Approved 的 Review #006。生产继续只表达行为相容迹象、偏移、需要复核或未校准，不宣称固定错误率目标已通过。后续若启用确定性强结论，必须使用独立冻结校准和 holdout。 |

### OI-016 — 全新生产界面与数据重置

| 字段 | 内容 |
|------|------|
| Phase / 事项 | Production UI / data lifecycle |
| 状态 | `Resolved` |
| 阻塞或人工验证原因 | 用户要求将产品作为全新版本展示，并删除供应商配置之外的业务数据。 |
| 已尝试方法及结果 | 已移除版本、探索、Legacy、暂停和 Enrollment 迁移文案；保留 3 个供应商和 3 个加密密钥；删除 8 条历史、2 个本地参考和全部验证/参考/校准/预算/分享/设置数据。供应商 hash 不变，SQLite integrity 为 ok。 |
| 建议后续操作 | Review #006 已于 2026-08-03 通过用户生产环境人工验收。清理前备份位于 `/root/backups/model-verity/fresh-start-20260728T120854Z-3297349`。 |

### OI-014 — UI Review #005 综合候选验收

| 字段 | 内容 |
|------|------|
| Phase / 事项 | UI / 安全删除、后台任务恢复、连接测试、结果证据与 Review #003/#004 整合 |
| 状态 | `Superseded by Review #006 Approved` |
| 阻塞或人工验证原因 | Review #005 的范围已被全新生产界面 Review #006 取代。 |
| 已尝试方法及结果 | Grill-me 50 项决策收束；代码实现、23 tests、typecheck、build、npm audit、候选 health/API/SPA smoke 通过；后续修订均纳入生产 Review #006，用户于 2026-08-03 确认验证正常并批准。 |
| 建议后续操作 | UI 人工验收项已关闭；npm 发布仍需单独批准。 |

### OI-010 — UI Review #004 导航与滚动

| 字段 | 内容 |
|------|------|
| Phase / 事项 | UI / 桌面滚动分区与页面返回可逆性 |
| 状态 | `Superseded by Review #005` |
| 阻塞或人工验证原因 | 未单独批准；已纳入 Review #005 最终候选统一验收。 |
| 已尝试方法及结果 | 实现导航栈、结果/历史/Enrollment 箭头返回、“返回验证”、桌面仅主区滚动；隔离与生产 Chromium 在 1280×800、390×844 通过；17 tests/typecheck/build 通过；候选已部署生产。 |
| 建议后续操作 | 用户在生产域名检查：桌面侧栏固定、历史→详情→返回、验证结果→返回验证/再测一次；批准后把 Review #004 标为 Approved，再与 #003 一并纳入 npm `0.1.1` 范围。 |

### OI-009 — 移动端 Basic Auth 登录

| 字段 | 内容 |
|------|------|
| Phase / 事项 | 生产访问 / iOS Chrome Basic Auth 连续 401 |
| 状态 | `Resolved` |
| 阻塞或人工验证原因 | 失败会话已到达 Nginx，但 18 次请求均未通过 htpasswd；旧随机凭据含易混淆字符，HTTP→HTTPS 跳转不携带 Authorization，iOS Chrome 还会按 realm 缓存失败凭据。 |
| 已尝试方法及结果 | 保留原账户，新增无 `0/O/1/I/l/i` 的移动账户，轮换 realm，验证 NPM 数据库、htpasswd、Authorization 清除和容器重启持久性；自动 iOS Chrome 首页/API 均 200。2026-07-25 用户在真实移动设备确认登录成功。 |
| 建议后续操作 | 始终使用 HTTPS 直达入口；凭据或 realm 轮换后重复真实设备验证；凭据仅保存在 root-only 文件。 |

### OI-008 — GPT-5.6 / Claude Fable 5 可信内置指纹

| 字段 | 内容 |
|------|------|
| Phase / 事项 | 研究数据 / 新模型内置参考 |
| 状态 | `Blocked on trustworthy sampling` |
| 阻塞或人工验证原因 | OpenAI/Anthropic 官方文档和 OpenRouter 当前目录已有 GPT-5.6 / Claude Fable 5，生产 9router `/models` 也返回相关路由；但 Zenodo DOI `10.5281/zenodo.21278557` 最新且唯一版本没有这些模型的 PAMELA 归一化采样记录。模型目录元数据不能生成经验分布。 |
| 已尝试方法及结果 | 联网核验 OpenAI、Anthropic、OpenRouter、Zenodo versions API 与本地 502 MB 研究归档；未发现可复用的 40 cells × 每 cell ≥10 有效样本。未伪造或复制相邻模型指纹。 |
| 建议后续操作 | 使用可信官方 endpoint 按 PAMELA 完整协议采集，固化整数 counts、来源、日期、模型版本和许可；通过质量门槛后再以新的 library/protocol 版本发布。现阶段可从 Base URL 发现模型 ID，并建立明确标为“自建”的本地参考。 |

### OI-007 — UI Review #003 与生产部署

| 字段 | 内容 |
|------|------|
| Phase / 事项 | UI / 内置研究来源展示与移动端防溢出修订 |
| 状态 | `Superseded by Review #005` |
| 阻塞或人工验证原因 | 未单独批准；已纳入 Review #005 最终候选统一验收。 |
| 已尝试方法及结果 | Chromium 在五档 viewport 完成隔离检查；候选已部署生产。本轮分页居中/菜单间距、供应商模型 ID 手填 + 在线发现又在 320×568、390×844、768×1024 通过生产数据副本和公网验证；9router 真实发现多个 GPT-5.6 和 Claude Fable 5 路由。移动端 Basic Auth 连续 401 已修复，用户于 2026-07-25 在真实移动设备确认登录成功。无横向溢出或 page error。17 tests/typecheck/build/audit 均通过。 |
| 建议后续操作 | 用户在生产域名检查 References、Verify 下拉、History→Result、长内容与小屏滚动；批准后把 Review #003 标为 Approved，再发布 npm `0.1.1`。 |

### OI-006 — 发布 npm 0.1.1 同步当前 UI

| 字段 | 内容 |
|------|------|
| Phase / 事项 | 发布维护 / 将 `verification-ui@v1.2` 同步到 npm registry |
| 状态 | `Open` |
| 阻塞或人工验证原因 | npm registry 的不可变版本 `model-verity@0.1.0` 不含当前候选；新 UI 需 Review #005 人工批准。 |
| 已尝试方法及结果 | npm README 已改为包内自足并加入 `第三方声明.md`；内置库可复现构建、17 tests、typecheck、build、官方 registry audit、移动 Chromium 检查已通过。未覆盖 `0.1.0`，符合 npm 不可变版本规则。 |
| 建议后续操作 | Review #005 批准后执行 `npm version patch` 和 `./scripts/publish-npm.sh`；发布后验证 tarball 含内置数据/第三方声明，并做独立全局安装与 API smoke。 |

### OI-005 — 公网域名生产切换

| 字段 | 内容 |
|------|------|
| Phase / 事项 | Phase 7 / `https://modelverity.example/` 部署验证 |
| 状态 | `Resolved` |
| 阻塞或人工验证原因 | 原域名指向 Vite 5173，生产 API 未接入；直接公开 API 还会暴露供应商密钥管理能力。 |
| 已尝试方法及结果 | 部署 Node 22 + systemd 服务，upstream 绑定 NPM Docker bridge `172.19.0.1:8787`；Proxy Host 9 切换到生产 server，并增加 HTTPS 强制跳转、Basic Auth、Block Exploits 与 Authorization 清除。重启 NPM 后配置仍持久，公网健康检查返回 `model-verity 0.1.0`；旧 Vite 5173 已停止。 |
| 建议后续操作 | 定期备份 `/var/lib/model-verity`；Basic Auth 凭据位于 root-only 文件；升级 npm 版本时先在测试端口验证，再重启 systemd。 |

### OI-004 — npm 首次公开发布

| 字段 | 内容 |
|------|------|
| Phase / 事项 | Phase 7 / 发布 `model-verity` 到 npm registry |
| 状态 | `Resolved` |
| 阻塞或人工验证原因 | 首次尝试因发布写操作缺少 2FA 授权返回 E403。 |
| 已尝试方法及结果 | 用户使用安全脚本输入新的短期 token 后成功发布 `model-verity@0.1.0`；官方 registry 元数据、tarball URL和独立全局安装均验证通过，CLI 输出 `0.1.0`。 |
| 建议后续操作 | 撤销发布用短期 token；后续版本先 `npm version patch/minor`，再运行同一发布脚本。 |

### OI-003 — 当前主机 keytar 原生模块不可加载

| 字段 | 内容 |
|------|------|
| Phase / 事项 | Phase 5 / K4 系统 keychain 优先路径 |
| 状态 | `Mitigated` |
| 阻塞或人工验证原因 | `keytar@7.9.0` 安装成功，但加载时报 `ERR_DLOPEN_FAILED`；当前 Ubuntu 环境缺少兼容的 libsecret/原生运行条件。 |
| 已尝试方法及结果 | 将 keytar 作为 optional dependency 动态加载；失败时自动使用 AES-256-GCM 本机加密文件。不会阻塞密钥保存。 |
| 建议后续操作 | 发布环境安装 libsecret 并验证 keychain；本机继续使用权限为 0600/0700 的加密文件回退。 |

## 已解决记录

### OI-001 — Node 开发依赖被生产环境安装清理

| 字段 | 内容 |
|------|------|
| Phase / 事项 | Phase 3 / Node 构建与类型检查 |
| 状态 | `Resolved` |
| 阻塞或人工验证原因 | 环境设置 `NODE_ENV=production`，单独安装运行依赖时 npm 清理 devDependencies，导致 `tsc: not found`。 |
| 已尝试方法及结果 | 执行 `npm install --include=dev`，恢复完整构建工具链；类型检查与构建通过。 |
| 建议后续操作 | 开发与 CI 始终使用 `npm ci --include=dev`；文档已注明。 |

### OI-002 — CLI 构建产物重复 shebang

| 字段 | 内容 |
|------|------|
| Phase / 事项 | Phase 3 / 可执行 CLI 构建 |
| 状态 | `Resolved` |
| 阻塞或人工验证原因 | 源文件和 tsup banner 同时添加 shebang，Node 报 `SyntaxError`。 |
| 已尝试方法及结果 | 删除 tsup banner，保留源码 shebang；CLI help/start/status/open/stop 全部验证通过。 |
| 建议后续操作 | 发布检查继续执行打包后 CLI smoke test。 |

### OI-012 — 历史记录删除功能

| 字段 | 内容 |
|------|------|
| Phase / 事项 | UI / 历史记录删除（单条 + 批量） |
| 状态 | `Resolved` |
| 描述 | 新增检查框选择、全选和服务端 `DELETE /api/runs/:id` / `POST /api/runs/delete`（原子事务，跳过活跃）；初版 `window.confirm` 已由 Review #005 应用内确认对话框取代。 |
| 提议后续操作 | 无需后续，已部署生产。 |

### OI-013 — 审计评分与可信度映射修复

| 字段 | 内容 |
|------|------|
| Phase / 事项 | Core / 审计抽样与分数等级映射 |
| 状态 | `Resolved` |
| 描述 | 根因一：审计从参考库随机抽 cell，可能选到 nValid<10 的 cell → 仅 1 cell 可比 → 被 `comparableCells < minCellsOk` 压为 inconclusive。根因二：UI 将 inconclusive 固定输出"证据不足或部分接近，建议复核"，不区分"距离低但 cell 不足"与"实际距离中等"。修复：审计只选参考 nValid≥10 的 cell；采用自适应 minValidForRepetitions；新增 classifyVerdictDetailed 输出细化文案和 scoreWouldMatch；UI 据此展示更准确描述。 |
| 提议后续操作 | 已部署生产；用户可重新测试同一场景确认评分与等级一致。 |

## 记录模板

### OI-XXX — 标题

| 字段 | 内容 |
|------|------|
| Phase / 事项 | |
| 状态 | `Open` |
| 阻塞或人工验证原因 | |
| 已尝试方法及结果 | |
| 建议后续操作 | |
