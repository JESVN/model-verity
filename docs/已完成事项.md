# DONE — 已完成项

按时间倒序追加。仅记录**已真实落地**的工作，不把计划当完成。

---

## 2026-08-05 — 内置参考在线更新下载体验修复

- [x] 服务端：AbortSignal 贯穿 `downloadToFile`/`fetchJson`，取消即刻中止传输（原取消仅标记状态、网络继续跑完）；下载期间按 MB 定期上报进度（原只有开始 5%/结束 45%）
- [x] 前端：进度文字移出进度条（原叠在 10px 条上不可读）；准备面板改用独立类名（原复用 `reference-version-heading` 结构不匹配导致文字错乱）；取消按钮即时反馈；轮询间隔 1.5s→1.2s
- [x] 新增回归测试：慢速流式 mock 验证下载中进度上报、取消及时中止、部分文件清理
- [x] 76 tests、typecheck、build、smoke 全过；已部署生产，post-check health/status、update 端点、SQLite integrity ok、维护锁清除；备份 `deploy-20260805T150254Z-2411143`

---

## 2026-08-05 — 内置研究参考在线更新（Zenodo）

- [x] 新增 `src/core/v2/zenodo-library.ts`（与 `build-builtin-library.mjs` 同语义的 40-cell 聚合/门槛/建库）与 `src/server/zenodo-update.ts`（检查更新、数据集下载缓存、prompt 一致性校验、按模型选择应用、版本回滚、缓存清理）
- [x] 新增 API：`/api/v2/references/update/{status,check,prepare,jobs/:id,jobs/:id/cancel,rollback,cache/clean}`；`builtin-library.ts` 支持数据目录运行时库覆盖层（dist 基线不变，更新后失效缓存）
- [x] 参考样本页新增“内置研究参考更新”面板：检查更新 → 下载并准备新数据集（首次数百 MB，缓存上限 2GB，一键清理）→ 勾选模型更新；同 ID 模型整体替换新快照，保留最近 3 版本可回滚
- [x] 安全：Zenodo 下载逐跳 SSRF 校验、2GB 大小上限、`MODEL_VERITY_ZENODO_PROXY` 可选代理（默认直连）、活动任务互斥 409、应用前自动备份；新数据集 `prompts_sha256` 与本地电池不一致时拒绝更新（避免跨 prompt 不可比）
- [x] 新增依赖 `undici`（代理）、`yauzl`（zip 流式读取）
- [x] 测试：核心聚合/门槛/建库 + mock Zenodo 全流程（检查→准备→应用→回滚→清理；prompt 不一致拒绝；未准备/空选择拒绝）；75 tests、typecheck、build、smoke、audit 全部通过
- [x] 更新 `README.md`（在线更新说明）、`AGENTS.md`（维护说明与证据边界）
- [x] 已部署生产：`scripts/deploy-production.sh`（`ALLOWED_HOST=modelverity.lwqnotop.cn`，因开源脱敏后脚本默认 host 为占位符，首跑 421 失败自动回滚后带真实 host 重跑成功）；`/opt` 预装 `undici@6.28.0`/`yauzl@3.4.0`；post-check systemd active（0.2.0）、health/status、`/api/v2/references/update/status` 返回打包基线 33 模型、3 供应商、SQLite integrity ok、维护锁已清除；备份 `deploy-20260805T124352Z-2371624`

---

## 2026-08-05 — 内置参考缩减至 33 个主流模型并部署生产

- [x] 内置研究参考库从 146 缩减到 33 个，仅保留 claude、openai、gemini、deepseek、kimi、qwen、glm、grok 八个主流厂商的当前代模型；移除 ai21、amazon nova、baidu、bytedance-seed、cohere、deepcogito、gemma、ibm、inception、inclusionai、liquid、llama、phi、minimax、mistral、nous、nvidia、perceptron、reka、tencent、upstage、writer、xiaomi 及各家旧代模型
- [x] 保留清单：claude-opus-4.7/4.8、claude-sonnet-4.6/5；gpt-5-chat/5.1/5.2/5.4/5.4-mini/5.4-nano/5.5；gemini-3.1-flash-lite、gemini-2.5-flash；deepseek-chat/chat-v3.1/v3.2/v4-flash；kimi-k2.5/k2.6；qwen-plus、qwen3-max、qwen3.5-plus-20260420、qwen3.6-plus/flash、qwen3.7-max/plus；glm-4.6/4.7/5/5.1/5.2；grok-4.20/4.3
- [x] 原 146 模型数据备份于 `src/data/builtin-fingerprints.json.gz.bak-146`；删除的模型仍可从 Zenodo 原始数据集恢复
- [x] 测试与 smoke 引用改为 `openai/gpt-5.4-mini`（原 `gpt-4o-mini` 已移除）；`test/server.test.ts`、`test/e2e-run.test.ts`、`scripts/smoke-v2-candidate.ts` 数量断言 146→33
- [x] 同步当前状态文档：`README.md`、`第三方声明.md`、`开发进度.md`、`后续路线图.md`、`对抗式检查.md`；历史文档不改写
- [x] TypeScript、68 tests、build 与 V2 candidate smoke 通过
- [x] 已部署生产：`scripts/deploy-production.sh`；post-check systemd active、health/status、`/api/references` library.models=33、3 个供应商保留、SQLite integrity ok、维护锁已清理；备份 `deploy-20260805T081129Z-2287498`

- [x] 查明真相：后端 600 次并发采样 p50 5.5ms/p95 33ms/p99 79ms/max 162ms，服务本身快，无持续阻塞；先前一批 462/212/104ms 是重启后一次性冷启动
- [x] 共因：openresty 整站 `auth_basic` 用 bcrypt `$2a$10$`，nginx 每请求重算哈希不缓存，实测 ≈132ms/请求，仅 2 worker；叠加 app 首次加载 6-7 个、每次保存 ~5 个往返请求，穿代理后普遍数百 ms
- [x] 修复：新增 `GET /api/bootstrap`（一次返回 providers/references/runs/status）；`App.refresh()` 改用单个请求。首次加载 7→3，保存后刷新 4→1，穿代理往返大幅减少
- [x] 顺带清理：客户端无调用方的 `api.providers()/references()/v2Runs()/status()` 死代码移除（服务端对应端点保留，供部署检查/诊断）
- [x] TypeScript、68 tests、build 与 candidate smoke 通过；已部署，post-check health/status、bootstrap 字段完整、SQLite integrity ok、维护锁清除
- [x] 2026-08-05 用户在生产环境人工验证加载与保存明显加快，Review #010 Approved

## 2026-08-05 — 首次加载/保存数据慢：重复 scrypt 派生与无超时请求

- [x] 第一性归因：`/api/providers` 每次读/写都会为每个 provider 重新执行内存昂贵 KDF（scrypt ≈50-60ms/次），3 个 provider 同步读 ~150ms，为其它端点 10+ 倍；密钥与 salt 恒定，纯属纯重复计算。另：App 首屏 `refresh()` 的 4 个 fetch 无超时，任一请求挂起则“正在加载验证数据”永久停留
- [x] `SecretStore` 增加按进程缓存的 AES 密钥（按 salt）与解密文档缓存，`set/delete` 时失效；行为与安全不变（同一密钥），加密格式不变
- [x] 前端 `request()` 增加 15s 有界超时（AbortController），超时给出可操作中文提示，首屏/保存不再无限等待
- [x] 基准：修复后 60 次密钥读取 0.3ms（原 ~9s）；生产 `/api/providers` 由 ~150ms 降至 ~2ms
- [x] TypeScript、68 tests、production build 与 V2 candidate smoke 通过
- [x] 已部署生产：`dist/server/index.js` / `index-NMO8OaqX.js`；post-check health/status、providers 计时、SQLite integrity ok、维护锁已清理

## 2026-08-04 — 验证页参考样本来源筛选

- [x] 用户反馈内置研究与自建参考混在同一列表难以选择
- [x] 参考样本 Select 上方增加“全部 / 自建参考 / 研究参考”筛选，显示各自数量
- [x] 切换筛选只展示对应来源，并自动选中该来源下最匹配的参考；空自建时给出创建指引
- [x] 筛选状态默认“全部”，不改变已保存参考选择和 History“再次验证”的资源缺失逻辑
- [x] 新增来源筛选回归测试；TypeScript、68 tests、production build 和 V2 candidate smoke 通过
- [x] 已部署生产：`index-IJMiDkPo.js` / `index-8VsZv4RY.css`；备份 `/root/backups/model-verity/deploy-20260803T173640Z-1638817`
- [x] systemd active/running，`NRestarts=0`；health/status/policy、旧写 API 410、SQLite integrity/quick check、资源 hash 和维护锁通过
- [x] 生产保留 3 个供应商和 1 个自建参考；未调用真实供应商、未发布 npm

## 2026-08-04 — 参考状态颜色、选中反馈与即时切换

- [x] 定位状态不更新根因：按钮无选中态、前端依赖额外 reload 且未阻止并发点击、过期操作保留旧质量状态
- [x] 当前可用/仍可使用/已过期/待复核/已暂停分别使用绿色、柔和绿、黄色、品牌橙和中性灰，并同时显示圆点、文字、时效/质量 badge
- [x] 四个治理按钮增加单选式对勾、边框、底色和 `aria-pressed`；点击后立即更新状态并锁定其他操作
- [x] 治理 API 返回 append-only event 和转换后的完整版本；失败回滚，成功按服务端版本校正
- [x] “标记为过期”改为 `freshness=stale, quality=approved`，避免旧待复核/暂停状态遮盖新选择；版本仍不可用于新验证
- [x] 新增 effective status、确定性转换和四状态 HTTP E2E 回归；TypeScript、67 tests、production build、V2 candidate smoke 和 dependency audit 通过
- [x] 已部署生产：`index-Bg-mSy5I.js` / `index-D8ErvPNs.css`；备份 `/root/backups/model-verity/deploy-20260803T163556Z-1621098`
- [x] systemd active/running，`NRestarts=0`；health/status/policy、旧写 API 410、SQLite integrity/quick check、资源 hash 和维护锁通过
- [x] 部署未触发治理操作，自建参考保持 `current/approved`；仅生产 `127.0.0.1:8787` 监听，无临时服务遗留
- [x] HTTPS 未认证首页/API 返回 401，HTTP 返回 301；未调用真实供应商，未发布 npm
- [x] 2026-08-04 用户在生产环境确认状态颜色、即时切换和来源筛选正常，Review #009 Approved；Review #007/#008 UI 人工项一并关闭

## 2026-08-03 — 参考样本信息架构与版本治理布局

- [x] 将自建参考与公开研究样本库拆分为两个独立卡片，消除创建、治理、研究搜索和列表的平铺混杂
- [x] 自建参考卡集中展示参考组/版本数量、创建入口、版本选择、当前时效/质量状态及治理操作
- [x] 明确治理状态只影响后续验证，不修改旧记录；研究搜索与匹配数量、结果、分页保持同一卡片上下文
- [x] 增加 720px/600px 响应式规则；窄屏标题、统计、状态和创建操作改为单列，主要触控目标保持至少 44px
- [x] 新增参考页分区布局回归测试；TypeScript、66 tests、production build、candidate smoke 和 dependency audit 通过
- [x] 已部署生产：`index-BqdFqZID.js` / `index-D3DIkwwF.css`；备份 `/root/backups/model-verity/deploy-20260803T154402Z-1605439`
- [x] systemd active/running，`NRestarts=0`；health/status/policy、旧写 API 410、SQLite integrity/quick check、资源 hash 和维护锁通过
- [x] 生产保持 3 个供应商、146 个研究参考和 1 个自建参考；未迁移或改写业务记录
- [x] HTTPS 未认证首页/API 返回 401，HTTP 返回 301；仅生产 `127.0.0.1:8787` 监听，候选/Vite/Preview 服务均已关闭
- [x] 未调用真实供应商、未修改评分/预算/质量门禁、未发布 npm；Review #008 等待用户人工验收

## 2026-08-03 — 自建参考表单语义去重与分步确认

- [x] 将旧平铺表单重组为“从哪里采集 / 这份参考代表什么 / 选择采集规模”三个步骤
- [x] 区分请求中的模型 ID 与用户声明的实际上游模型；默认联动，网关别名按需展开，人工修改后不被模型选择覆盖
- [x] 将模型列表获取合并到模型 ID 控件；远程未配置模型显示状态并禁止绕过供应商 allowlist 启动
- [x] 参考显示名称按供应商、上游模型和月份自动生成；自定义名称、协议覆盖和临时 Key 收入高级设置
- [x] 移除固定 OpenAI 默认声明；切换供应商清除旧上游厂商；启动前显示请求路径、身份、产品形态和来源等级摘要
- [x] 修复参考采集完成/取消后创建表单未退出，以及取消时轮询 timer 未清理的相邻状态问题
- [x] 新增自动名称、模型联动、远程 allowlist 和表单语义回归测试；TypeScript、65 tests、production build、candidate smoke 和 dependency audit 通过
- [x] 已部署生产：`index-BwfwFXeS.js` / `index-CQB2NMqV.css`；备份 `/root/backups/model-verity/deploy-20260802T184243Z-1254531`
- [x] systemd active；health/status/policy、3 个供应商、146 个参考、SQLite integrity/quick check、资源 hash、维护锁和临时监听检查通过
- [x] 未调用真实供应商、未修改评分/预算/质量门禁、未发布 npm；Review #007 的未完成人工验收后由 Review #008 合并承接

## 2026-08-03 — 供应商长元数据操作区布局修复

- [x] 定位随想供应商布局异常：多个模型和较长元数据增大 Flex 基准宽度，操作区被压缩后中文按钮文字换行
- [x] 为供应商操作区增加独立样式，禁止操作区收缩并保持“测试连接”“编辑”单行显示；移动端继续按现有规则换行排列操作组
- [x] 增加长元数据操作区回归测试；TypeScript、61 tests、production build、candidate smoke 和 dependency audit 通过
- [x] 已部署生产：`index-BpfOVBah.js` / `index-HOLV30RT.css`；备份 `/root/backups/model-verity/deploy-20260802T165832Z-1224118`
- [x] systemd active；上游 health/status、API policy、3 个供应商、146 个参考、SQLite integrity/quick check、资源 hash 和维护锁检查通过
- [x] Basic Auth 由用户确认已自动恢复；公网未认证 HTTPS 返回 401，HTTP 继续 301 跳转 HTTPS
- [x] 候选、Vite 和 smoke 临时监听均已移除，候选临时目录已清理；未发布 npm，未调用真实供应商
- [x] 2026-08-03 用户确认操作区布局正常，Review #006 Approved

## 2026-08-01 — 参考样本表单：模型列表获取与产品形态说明重写

- [x] 创建参考样本表单新增“从模型列表接口获取”选项：从所选可信供应商读取服务声明的模型 ID，选择后填入参考端模型；复用已保存密钥，不修改供应商配置
- [x] 获取结果明确“列表只表示服务声称支持这些 ID，不证明模型身份”；已配置的模型在选项中标注
- [x] 重写“产品形态”五个选项的说明：解释它指模型以哪种方式被提供和使用（API、网页产品、编程产品、企业部署），同一模型在不同形态下回答可能不同，比较时目标与参考应选同一形态
- [x] 字段名改为“产品形态（使用方式）”，帮助文案补充选择影响；选项说明保持 20–55 字简洁约束
- [x] TypeScript、60 tests、production build、candidate smoke 和 dependency audit 通过；未调用真实供应商
- [x] 已部署生产：`index-Dxk-rsiN.js` / `index-BKiiD5YA.css`；备份 `/root/backups/model-verity/deploy-20260731T174016Z-432647`
- [x] systemd active；health/status、API policy、供应商读取和 SQLite integrity 检查通过；公网/安装目录/本地资源 hash 一致；维护锁已清除
- [x] 参考表单与产品形态说明已纳入 2026-08-03 Approved 的 Review #006

## 2026-08-01 — 供应商编辑弹窗保存后自动关闭

- [x] 编辑弹窗“保存修改”成功后自动关闭并刷新列表；保存失败时保持弹窗打开，错误原位显示
- [x] 添加供应商表单行为不变（保存后清空表单并显示提示）
- [x] TypeScript、60 tests、production build、candidate smoke 和 dependency audit 通过；未调用真实供应商
- [x] 已部署生产：`index-CH0bGsuj.js` / `index-BKiiD5YA.css`；备份 `/root/backups/model-verity/deploy-20260731T172855Z-428386`
- [x] systemd active；health/status、API policy、供应商读取和 SQLite integrity 检查通过；维护锁已清除
- [x] 编辑弹窗桌面、移动和键盘检查已纳入 2026-08-03 Approved 的 Review #006

## 2026-08-01 — 供应商协议/地址变更不再要求重输 API Key

- [x] 移除“协议或 Base URL 变化必须重输 API Key”的限制：编辑供应商或获取模型列表时，未填 Key 直接沿用已保存加密密钥
- [x] 协议与地址是请求路径/字段描述，API Key 通常可跨协议复用；只有首次新增或确实换钥才需要填写
- [x] 模型列表获取同步沿用已保存密钥；未保存供应商仍需提供 Key
- [x] e2e 改为验证变更协议/地址后仍用已保存 Key 成功获取模型与保存供应商，并恢复原目标继续后续流程
- [x] TypeScript、60 tests、production build、candidate smoke 和 dependency audit 通过；未调用真实供应商
- [x] 已部署生产：`index-CL7QJ7uG.js` / `index-BKiiD5YA.css`；备份 `/root/backups/model-verity/deploy-20260731T172437Z-426378`
- [x] systemd active；health/status、API policy、供应商读取和 SQLite integrity 检查通过；维护锁已清除
- [x] 编辑弹窗与协议变更检查已纳入 2026-08-03 Approved 的 Review #006

## 2026-08-01 — 供应商编辑独立弹窗

- [x] 供应商页“编辑”按钮改为弹出独立对话框，不再在“添加供应商”表单中直接改写已有供应商，避免误操作
- [x] 编辑弹窗包含名称、Base URL、API Key（留空保留原密钥）、协议、角色、模型 ID 与模型列表获取；保存后关闭弹窗并刷新列表
- [x] 取消、弹窗外点击、Escape/背景锁定沿用现有 dialog 交互；保存中禁用关闭，错误原位显示在弹窗内
- [x] 弹窗布局复用管理表单双列网格，窄屏折叠为单列；不改变后端 API 语义
- [x] TypeScript、60 tests、production build、candidate smoke 和 dependency audit 通过；未调用真实供应商
- [x] 已部署生产：`index-CL7QJ7uG.js` / `index-BKiiD5YA.css`；备份 `/root/backups/model-verity/deploy-20260731T165550Z-416834`
- [x] systemd active；health/status、API policy、供应商读取和 SQLite integrity 检查通过；维护锁已清除
- [x] 编辑弹窗桌面、移动、键盘和焦点检查已纳入 2026-08-03 Approved 的 Review #006

## 2026-08-01 — 验证默认选择、浏览器偏好与全局加载状态

- [x] 首次根数据加载完成后再显示验证表单；默认选择第一个验证供应商、该供应商第一个模型和第一个参考样本
- [x] 保存验证方式、供应商/模型、参考、检测深度、协议和身份字段，刷新后恢复；失效资源回退到当前第一个可用项
- [x] 临时 API Key、预算授权、credential session 和运行状态不进入浏览器偏好；History“再次验证”资源缺失逻辑保持 fail-closed
- [x] 新增可全局复用的 `LoadingState`，用于根数据、参考治理数据和上次运行恢复；包含 ARIA 状态和 reduced-motion 静态降级
- [x] 新增默认选择、偏好 round-trip、敏感字段排除和加载状态回归测试
- [x] TypeScript、60 tests、production build、candidate smoke 和依赖审计通过；未调用真实供应商
- [x] 已部署生产：`index-DtKLjZlO.js` / `index-XjIFl9Df.css`；备份 `/root/backups/model-verity/deploy-20260731T163129Z-409144`
- [x] systemd active；上游/公网 health/status、API policy、3 个供应商、146 个参考、SQLite integrity/quick check 和公网新资源检查通过
- [x] 维护锁已清除，候选 `127.0.0.1:18787`、Vite `5173` 和候选临时目录均已关闭或清理
- [x] 首次默认、刷新恢复、资源失效、移动端、键盘和 reduced-motion 检查已纳入 2026-08-03 Approved 的 Review #006

## 2026-07-31 — 文案去重与全局文字颜色

- [x] 保存基线：22 条选项说明共 1111 字、10 条字段帮助共 580 字、4 条页面导语共 259 字
- [x] 精简后分别为 736 字（约 -34%）、278 字（约 -52%）和 147 字（约 -43%），保留选择条件、费用影响和证据边界
- [x] 评分组成移除与技术详情重复的逐项数值描述；技术详情缩短 JSD、覆盖、校准、尾部比例和清单解释
- [x] 全局文字颜色改为暖中性高对比层级：正文、次要文字、弱辅助文字在主要深色表面的最低对比度分别为 12.19:1、7.59:1、5.67:1
- [x] 成功、警告、危险及低分状态改用语义 token，最低对比度 4.96:1；暖橙主按钮改用深色文字，默认对比度 5.78:1
- [x] 更新回归测试，约束辅助文字、状态色、主按钮和选项说明长度；TypeScript、57 tests 和 production UI build 通过
- [x] 已部署生产：`index-BTXI50li.js` / `index-CFVSbpSn.css`；备份 `/root/backups/model-verity/deploy-20260731T145948Z-382372`
- [x] systemd active；上游/公网 health/status、API policy、3 个供应商读取、SQLite integrity/quick check、维护锁和公网新文案检查通过
- [x] 候选 `127.0.0.1:18787`、Vite `5173` 和临时候选目录均已关闭或清理；未遗留本地测试服务
- [x] 桌面、移动端、键盘和扫读效率检查已纳入 2026-08-03 Approved 的 Review #006

## 2026-07-31 — 小号文字可读性与普通语言解释

- [x] 保存样式基线：原界面有 2 处 9px、6 处 10px、22 处 11px、26 处 12px 声明；弱辅助色在深色卡片上的最低对比度约 2.86:1
- [x] 历史记录摘要提升为 13px、1.65 行高和中等字重，并增加标题/摘要间距与列表卡片间距
- [x] 全局次要文字和弱辅助文字提高对比度；主要深色表面上的最低对比度分别约 6.07:1 和 4.65:1
- [x] 字段说明、Select 副说明、预算、结果关键说明、评分组成和技术详情提高字号、字重、行高与间距；关键说明不再使用 11px 以下文字
- [x] 协议、供应商角色、模型 ID、产品形态、P1/P2/P3、L1/L2/L3、参考样本、预算、token、时效、质量和分享报告改为普通语言说明
- [x] 结果技术详情补充 JSD、有效问题覆盖、校准、重复抽样区间、同源样本尾部比例、异源样本误接近比例、提问方式和清单校验值的直白解释
- [x] 新增辅助文字对比度、字段/Select/History 字号和选项解释完整性回归测试
- [x] TypeScript、57 tests、production build 和 candidate smoke 通过；未调用真实供应商
- [x] 已部署生产：`index-CcmWJCWQ.js` / `index-Dvad-M3x.css`；备份 `/root/backups/model-verity/deploy-20260731T133620Z-358330`
- [x] systemd active；上游/公网 health/status、API policy、3 个供应商读取、SQLite integrity/quick check 和维护锁检查通过
- [x] 候选 `127.0.0.1:18787` 已关闭，临时数据目录已删除；未遗留 Vite、smoke 或其他本地服务
- [x] 桌面、320–430px、键盘、长 Select 和理解度检查已纳入 2026-08-03 Approved 的 Review #006

## 2026-07-31 — 高行为相似度与来源范围拆分

- [x] 只读复核生产 86.2 分运行：JSD 0.021452、bootstrap 0.000330–0.052266、80/80 成功、覆盖 100%、短时稳定；确认“需要复核”不是行为阈值导致，而是 P3 研究参考和无 calibration cap
- [x] 评分规则升级为 `pamela-scorecard@3.1.0`；不改五维权重、JSD 映射、等级阈值或 P3/未校准 cap
- [x] 对仅受证据范围限制且质量达标的结果，以行为维度分作为主“行为相似评分”，保留综合证据分，并拆分显示“行为高度/较为相似”与“来源证据/比较范围待复核”
- [x] 低成功率、低覆盖、行为混合、fallback、参考治理失败或行为差异较大继续保持“需要复核/可信度较低”
- [x] `pamela-scorecard@3.0.0` 历史记录保持不可变；界面基于冻结字段做等价展示，History 和结果页同步
- [x] P3 原始五维文案由“不可比”改为“仅作诊断比较”，明确行为距离可用但不能证明上游来源
- [x] JSON/Markdown/CSV 保留综合分、主展示分、band/cap 和来源范围标签；技术详情显示行为相似分、综合证据分、数值原始等级与 cap 后等级
- [x] TypeScript、54 tests、production build、candidate smoke 和 dependency audit 通过；未调用真实供应商
- [x] 已部署生产：`index-BulRbg95.js` / `index-P7SjsYD4.css`；备份 `/root/backups/model-verity/deploy-20260730T175818Z-17614`；systemd、health/status、API、SQLite integrity 和维护锁均通过
- [x] 修复双分数后的颜色语义冲突：scope-only 结果按主展示分使用 85/70/50 色阶，98 分为绿色；来源范围 badge 保持黄色，真实质量/稳定性 cap 继续按 cap 后等级着色
- [x] 新增 85、70、50 边界及实质 cap 不变绿的回归测试；TypeScript、55 tests、build、smoke、audit 通过
- [x] 新色阶已部署生产：`index-BzaIOLYr.js` / `index-m3t5jrG9.css`；备份 `/root/backups/model-verity/deploy-20260730T184222Z-30584`；systemd、health/status、API、SQLite integrity 和维护锁均通过
- [x] 桌面、移动和键盘检查已纳入 2026-08-03 Approved 的 Review #006

## 2026-07-30 — History 再次验证配置恢复

- [x] 修复 History 结果页“再次验证”只切换导航、清空结果，却没有把历史配置交给验证表单的问题
- [x] 恢复原验证方式、目标供应商/模型、参考样本或实时参考端、检测深度、协议及两端身份声明
- [x] 不恢复 API Key、临时凭据和旧预算授权；每次启动仍重新展示并批准当前请求预算
- [x] 原供应商、模型或参考已不可用时 fail-closed，保留安全默认并明确提示用户重新选择，不静默换成其他目标
- [x] 新增 screening、paired 和资源失效回归测试；Review #006 增加人工工作流检查项
- [x] 已部署生产：`index-_ZLHqgVn.js` / `index-DpnO7f4U.css`；备份 `/root/backups/model-verity/deploy-20260730T065351Z-4024564`；systemd、health/status、API、SQLite integrity 和维护锁均通过
- [x] History→结果→再次验证及焦点/滚动检查已纳入 2026-08-03 Approved 的 Review #006

## 2026-07-29 — V3 评分制验证与简化界面

- [x] 深入审计 VeriDrop commit `dca32815b7edd02bb347dfea51c35218fd88f604` 的 runner、detector、scorer、critical cap、报告与在线信息层级
- [x] 形成 `docs/VeriDrop开源仓库研究-20260729.md`，明确采用项、拒绝项和误判风险；未复制 AGPL 源码、题库或 UI
- [x] 先完成 `docs/v3实施计划.md`，冻结评分语义、权重、阈值、cap、界面和兼容策略
- [x] 新增 `src/core/v3/scoring.ts`：行为 55%、质量 15%、稳定性 10%、可比性 10%、参考强度 10%
- [x] 未校准行为分使用透明的 `100 × (1 − JSD)` 展示映射；不表述为真实性概率或错误率校准
- [x] P3、老化 L3、低质量、低覆盖、mixture/fallback 和参考质量状态使用独立结论上限，原始数值分不篡改
- [x] 新运行原生持久化 scorecard；无 V3 scorecard 的旧运行不进入当前 API/UI
- [x] 默认验证页简化为供应商、模型、参考、检测深度、预算和启动；实时配对及身份/协议覆盖收入高级验证方式
- [x] 页面顶部明确“验证结果仅供参考，无法保证百分之百准确”
- [x] 结果页首屏改为综合可信评分、等级、三条关键说明和五项评分条；JSD、bootstrap、原始五维和 manifest 默认折叠
- [x] History、JSON、Markdown、CSV 和 7 天时点报告接入评分卡
- [x] 新增 V3 单调性、阈值、critical cap 和无法评分测试
- [x] V3 初次发布通过 46 tests；移除旧兼容测试与路径后最终为 45/45，TypeScript、production build、candidate smoke 和 dependency audit 均通过；未调用真实供应商
- [x] 已部署生产：`index-BIYnOqH2.js` / `index-C37aSYsE.css`；备份 `/root/backups/model-verity/deploy-20260728T192034Z-3424603`
- [x] 用户决定不兼容旧业务数据；清理前完整备份 `/root/backups/model-verity/v3-fresh-start-20260728T201506Z-3440092`
- [x] 生产旧验证、观察、attempt、证据、结论、预算、分享、校准、旧参考和设置数据已全部清理
- [x] 供应商 3 个、供应商 hash 和两份密钥 hash 均保持不变；SQLite integrity `ok`
- [x] 前端停止读取 `/api/runs`，History 只显示当前评分运行；旧详情和派生评分路径已删除
- [x] 精简版本已部署：`index-CVufyNW5.js` / `index-C37aSYsE.css`；备份 `/root/backups/model-verity/deploy-20260728T202031Z-3442069`
- [x] 数据重置审计：`docs/V3生产数据重置-20260729.md`
- [x] 桌面、移动、键盘和用户理解度检查已纳入 2026-08-03 Approved 的 Review #006

## 2026-07-29 — 导航按钮实时进度恢复

- [x] 修复当前验证运行与应用壳状态断连：侧栏不再只读取已停用的旧运行结构
- [x] 验证进行时，“验证”按钮持续显示 `0%–100%`，切到供应商、参考样本或历史后仍实时更新
- [x] 点击带进度的“验证”按钮返回当前运行；刷新和其他标签页可恢复轮询
- [x] 自建参考采集同步在“参考样本”按钮显示进度
- [x] TypeScript、41 tests、production build、candidate smoke 和 dependency audit 通过；未调用真实供应商
- [x] 已部署生产：`index-BcoIZfFI.js` / `index-0H3_uVLj.css`；备份 `/root/backups/model-verity/deploy-20260728T182352Z-3406590`
- [x] 跨页面进度和完成徽标检查已纳入 2026-08-03 Approved 的 Review #006

## 2026-07-29 — 全站术语解释与可比性条件修复

- [x] 新增统一产品词典：协议、产品面、P1/P2/P3、L1/L2/L3、预算、供应商角色、时效、质量和运行状态
- [x] Select 支持选项副说明；字段下方统一显示“是什么、怎么选、影响什么”
- [x] 产品面改为中文服务形态：API 接口、ChatGPT 产品、Codex 编程产品、企业专属部署、无法确认
- [x] 协议可比性改为“严格可比/映射后可比/仅作诊断”，显示当前推荐和选择后果
- [x] 研究参考默认 P3；产品面 unknown、厂商/产品/产品面不一致时推荐并强制 P3
- [x] 实时配对补充参考端声明厂商、产品和产品面
- [x] 服务端 P1/P2 校验厂商、产品和非 unknown 产品面；P1 额外校验协议一致
- [x] 结果页解释五维证据、JSD、可比覆盖、同源校准尾部概率、异源误接近风险和采样清单校验值
- [x] 供应商角色、协议、模型发现、密钥后端、连接错误、参考治理和 History 状态中文化
- [x] 内部英文错误映射为可执行的中文提示
- [x] 新增 P1/P2 身份条件 API 回归测试；TypeScript、41 tests、production build、candidate smoke 和 dependency audit 通过
- [x] 已部署生产：`index-u_4omHRb.js` / `index-0H3_uVLj.css`；备份 `/root/backups/model-verity/deploy-20260728T172851Z-3390666`
- [x] 长解释、320–430px、Select、键盘和选择决策检查已纳入 2026-08-03 Approved 的 Review #006

## 2026-07-29 — 自建参考样本闭环修复

- [x] 不恢复旧 `/api/enroll`，新增当前框架 `POST /api/v2/references/enrollment-plans`
- [x] 从可信 reference/either 供应商选择模型和明确协议，用户逐批批准 20/80/240 次请求预算
- [x] 复用版本化 challenge manifest、结构化 observation、append-only attempt、原子请求/token 预算和取消
- [x] 质量门禁：成功率 ≥90%、可用 cells ≥75% 且至少 4、reasoning 关闭确认、冻结 manifest 完整完成
- [x] 通过后在同一 SQLite transaction 发布 reference cohort + immutable version 并完成任务状态
- [x] L2/L3 用户选择；禁止自建路径声明 L1
- [x] 自建版本自动进入参考样本比对；stale/review_required/quarantined/superseded 不可用于新验证
- [x] 直接 `POST /api/v2/reference-versions` 改为 410，禁止任意 fingerprint 绕过采集门禁
- [x] 空参考状态仍显示创建入口；支持进度、取消、刷新/多标签恢复、完成/失败反馈和版本治理
- [x] 本地 mock E2E 覆盖采集、质量、发布、列表接入、直接导入拒绝和 stale 移除
- [x] TypeScript、40 tests、production build、candidate smoke 和 dependency audit 通过；未调用真实供应商
- [x] 已部署生产；资源 `index-BJXpKaC9.js` / `index-WI1vRg2h.css`，备份 `/root/backups/model-verity/deploy-20260728T164908Z-3378535`
- [ ] 首次真实用户采集：需用户在 UI 明确批准供应商、模型和 20/80/240 次请求预算

## 2026-07-28 — 全新生产界面与数据重置

- [x] 移除生产 UI 中面向用户的 v2、探索模式、Legacy、暂停、Enrollment 和候选迁移文案
- [x] 验证页直接使用当前产品语义：综合服务声明验证、参考样本比对、实时双端配对
- [x] History 只展示当前框架新记录；初始状态为空
- [x] 参考页改为“参考样本”，移除旧 Enrollment 暂停卡片和迁移说明
- [x] 移除无可操作选项的设置导航和静态页面；数据与隐私说明保留在对应工作流附近
- [x] 保留 3 个供应商配置和对应 3 个加密密钥，供应商配置 hash 清理前后相同
- [x] 删除 8 条历史、2 个本地参考，以及所有验证、参考治理、校准、预算、分享和旧设置数据
- [x] 清理前完整备份：`/root/backups/model-verity/fresh-start-20260728T120854Z-3297349`
- [x] 生产资源更新为 `index-C_DpW4FZ.js` / `index-WI1vRg2h.css`
- [x] 设置页移除部署备份：`/root/backups/model-verity/deploy-20260728T144312Z-3341592`
- [x] TypeScript、39 tests、production build、dependency audit、SQLite integrity 和生产 smoke 通过
- [x] Review #006 于 2026-08-03 通过用户生产环境人工验收

## 2026-07-28 — 内部第二代框架实现历史

- [x] M0：Web 旧 Audit 和 Enrollment 入口暂停，Legacy 历史/结果明确标注未校准
- [x] M1：新增独立 v2 schema、observation/attempt/evidence revision、Legacy 聚合 API
- [x] M2：实现 Vendor/Product/Surface/Snapshot、L1/L2/L3、P1/P2/P3 数据边界
- [x] M3：实现 calibration artifact hash/1% 门禁、bootstrap mean interval、empirical conformal evidence
- [x] M4：实现研究参考 screening runner；无匹配 calibration 时拒绝强结论
- [x] M5：实现 paired challenge runner、字节一致 hash、预算上限、结构化双端 observation
- [x] M6：实现 v2 候选工作区、五维结果、进度/取消、JSON 时点导出
- [x] M7：新增 v2 core/repository/API/E2E 测试，全套增至 28 项；typecheck/build/audit 通过
- [~] M8：已按用户指定用 9router L2 reference 追加执行 880 次低预算矩阵；完成随机交错多 block、两目标 split-half、3 个行为阴性和 holdout。数据表明 GPT-5.5/GPT-5.4 不可可靠区分，AiCodeWith holdout 因预算实现缺陷作废；仍不能建立 1%/1% calibration
- [x] M8 协议消歧：同 endpoint/model Chat↔Responses 40 请求 crossover 得 JSD 0.058；确认协议映射有有限影响但不能解释 AiCodeWith 全部偏移
- [x] M8 零费用安全矩阵：完成 Chat/Responses semantic mapping、固定 model 字段的 block mixture、retry fallback、429 no-retry、并发预算及 token fail-closed 测试
- [x] M9.1–M9.4：探索模式硬门禁、原子预算、每端协议、内存临时凭据
- [x] M9.5–M9.8 后端：v2/Legacy History 分层、v2 cascade 删除、reference governance repository、share report、Legacy 410 环境开关
- [x] M9.9–M9.10：安全/mock 测试和生产数据库副本迁移演练；Legacy hash 不变、旧 reader 可用
- [x] M9 候选工程：完整报告、endpoint/HMAC 绑定、reference governance、v2 History 恢复/删除/多标签、pair startGap、协议 UI、临时 Key UI、SSRF policy 和独立候选 smoke
- [x] Review #006 已创建；当前状态见文件顶部“全新生产界面与数据重置”
- [x] M9 生产切换：2026-07-28 19:34 CST 部署，Basic Auth、health、policy、SPA、旧写 API 410、备份和数据 hash smoke 通过
- [x] 迁移期界面随后由全新生产界面替代，历史和用户生成业务数据按明确要求清空
- [x] Review #006 于 2026-08-03 通过用户生产环境人工验收；确定性强结论仍保持证据门禁，npm 未发布

## 2026-07-28 — v2 需求冻结

- [x] 完成 Grill-me 1–50，冻结第三方中转站综合服务声明验证目标
- [x] 冻结行为/来源/稳定性/协议/时效五维结果，取消 v2 单一可信度
- [x] 冻结 L1/L2/L3 参考、P1/P2/P3 协议及 Vendor/Product/Surface/Snapshot 身份层级
- [x] 冻结 Tier 1 官方 cohort + 用户第一方实时配对双轨方案
- [x] 冻结 profile/subset/cross-provider calibration、bootstrap/conformal evidence 与 1%/1% 强结论目标
- [x] 冻结 pair-aware 序贯 runner、预算授权、attempt 保留、阳性/阴性对照和动态路由检测
- [x] 冻结独立 v2 数据模型、Legacy 只读、临时第一方 Key 和时点分享报告
- [x] 冻结完整框架一次替换与 Review #006 发布门禁
- [x] 输出 `docs/v2实施计划.md`，包含 M0–M9、API/数据边界、测试矩阵和验收标准

## 2026-07-27 — 9router 进度条刷新修复

- [x] 按第一性原理确认问题在前端轮询写回条件：服务端状态已更新，刷新可见，说明 SQLite 与 run manager 正常
- [x] 修复轮询只依赖 `view.runId` 的门控；当当前 `activeRun.id` 匹配轮询 ID 时也写回最新 run
- [x] 保持查看无关 History 详情时不覆盖当前详情，只更新同一个 active run

## 2026-07-27 — OpenAI Responses 协议

- [x] 新增独立 `openai-responses` adapter 和供应商协议选项 `OpenAI Responses`
- [x] 使用 `POST <baseUrl>/responses`、`instructions`、`input`、`max_output_tokens`、`store:false`
- [x] 支持顶层 `output_text` 与 `output[].content[].output_text` 两种可见文本结构
- [x] 映射 `input_tokens` / `output_tokens`、响应 `model` 和 reasoning 状态
- [x] reasoning effort 被 endpoint 明确拒绝时去字段回退一次并标记协议降级
- [x] Responses 与 Chat Completions 显式分离，不在运行中自动切换，避免污染行为指纹
- [x] 供应商保存、模型发现、连接测试及 E2E 覆盖通过；连接测试弹窗可独立选择协议且不改写供应商配置，结果固化实际请求协议；全套测试增至 23 项

## 2026-07-27 — Review #005 候选实现

- [x] Grill-me 逐项收束 50 个产品、安全、状态恢复、部署和验收决策；按第一性原理拆分“任务真实状态、当前查看详情、浏览器定位提示”
- [x] 历史单条/批量、供应商、本地参考统一使用 44×44px、默认无外框无底色的红色危险垃圾桶图标与应用内确认对话框；默认焦点取消、Escape/焦点锁定、提交期间不可关闭、失败原位反馈
- [x] Audit/Enrollment 使用独立 `localStorage` 跟踪 ID 与已查看状态；跨导航、刷新、重开和多标签页恢复；History 详情不覆盖后台任务
- [x] 导航显示运行百分比、中性“已完成”或“需查看”，不提前编码可信度
- [x] 新增已保存供应商最小推理连接测试：20 秒、临时内存会话、精确取消、与采样双向互斥、结构化脱敏诊断、不保存正文或 History
- [x] 结果固化运行时判定快照、供应商/参考/协议摘要、原始样本计数、错误分类、响应 model 分布、纳入/排除 cell；旧历史不反推
- [x] 结果 UI 增加 3 位 JSD 刻度、决定性原因、证据质量检查、原因对应建议、运行信息与两层证据
- [x] Markdown/CSV/JSON 导出同步新增证据；CSV 继续防公式注入
- [x] 新增 root-only 维护锁语义和 `scripts/deploy-production.sh`：候选 smoke、任务等待、备份、切换、失败回滚，不调用真实供应商、不发布 npm
- [x] Review #005 建档并取代未批准 #003/#004；后续由 2026-08-03 Approved 的 Review #006 覆盖
- [x] 23 tests、web/node typecheck、production build、npm production audit（0 vulnerabilities）、候选 health/API/SPA smoke 通过
- [x] 生产候选按维护锁/备份/回滚门禁部署；上游与公网 Basic Auth 后首页/API 200、未认证 401；备份 `/root/backups/model-verity/deploy-20260727T115035Z-2882949`

## 2026-07-27 — 历史删除功能与审计评分修复

- [x] 新增历史记录单条删除、批量勾选删除、全选删除；初版使用 `window.confirm`，已由 Review #005 的应用内确认对话框取代
- [x] 新增 DELETE /api/runs/:id、POST /api/runs/delete（批量）两个后端端点
- [x] SQLite 新增 deleteRun() 和 deleteRuns()，跳过活跃 run、原子事务提交
- [x] 根因：审计评分异常来自两条:
  1. 审计从参考库任意抽 cell，但参考可能包含有效样本不足（nValid<10）的 cell → audit 筛选 1 个可比 cell，被质量门禁压制为 inconclusive
  2. UI 把 inconclusive 误译为"中等可信"，让人产生"分数低却中等可信"的错觉
- [x] 修复（第一性原理）:
  - 审计只选参考库中 nValid≥10 的 cell 抽样，避免一开始就抽到无效 cell
  - 对比阶段使用自适应 minValidForRepetitions(reps)：Quick 10→7、Audit 15→10、Full 30→10，避免 Quick（10 次）被刚性 10/10 门槛淘汰
  - 新增 classifyVerdictDetailed，返回 reason 和 scoreWouldMatch，inconclusive 时区分"距离很低但 cell 不足" vs"距离在中位带"
  - UI 不再统一显示"证据不足或部分接近，建议复核"，改为根据 reason 给出更具体的描述

## 2026-07-25 — 页面切换卡顿修复（历史实现，设置页已于 2026-07-28 移除）

- [x] 第一性原理定位：当时的主导航切换会卸载/重挂载页面，静态设置页还会重新请求，视觉上表现为“点一下卡一下才切过去”
- [x] 当时将验证/供应商/参考/历史/设置改为 keep-alive；当前设置页已移除，其余四个页面继续使用 `hidden` 切换
- [x] 恢复轻量 160ms 入场动画（短位移 + 透明度），不再与整页 remount 叠加
- [x] 列表项去掉 `backdrop-filter` 与 hover 位移；滚动恢复继续使用 `useLayoutEffect`
- [x] 生产实测 keep-alive 生效、切换约 23–47ms、历史返回与滚动分区回归通过并已部署

## 2026-07-25 — 导航可逆性与桌面滚动分区

- [x] 桌面壳层改为视口固定：仅 `.main` 独立纵向滚动，顶栏与左侧导航不随内容滚动
- [x] 引入页面内导航栈：记录来源分区、滚动位置，支持详情/结果返回上一页
- [x] 验证结果页增加箭头返回和“返回验证”；“再测一次”回到验证表单
- [x] 历史记录进入详情后可通过“返回历史”回到列表，并尽量恢复原滚动位置
- [x] Enrollment 详情同样提供箭头返回，区分来自参考指纹或历史
- [x] 1280×800 / 390×844 隔离与生产 HTTPS Chromium 检查通过；17 tests/typecheck/build 通过
- [x] 生产部署并保留回滚：`/opt/model-verity-rollback-20260725-005406`

## 2026-07-30 — GPT-5.5 内置参考可比性修正

- [x] 从生产已保存证据复核 76.7/79.6 分组成，未调用真实供应商
- [x] 修复内置 PAMELA 比较错误追加 `Request marker`；内置比较恢复原始固定 prompt
- [x] 实时配对和自建参考继续使用 marked prompt，保持双方协议一致
- [x] 内置参考时效改为冻结的 0–14 天 current、15–45 天 usable、超过 45 天 stale，不再无条件 stale
- [x] 新增固定 prompt 和 14/45 天边界回归测试；全量 47/47 通过
- [x] manifest 升级为 `pamela-challenge@2`，在 manifest、结果 JSON 和技术详情显式保存/显示 `promptMode`
- [x] 旧运行及其 77 分保持不可变；修复仅影响新运行
- [x] 已部署生产：`index-BOx-PE_M.js` / `index-UKCJhZJ7.css`；备份 `/root/backups/model-verity/deploy-20260729T182845Z-3816014`

## 2026-07-30 — 生产域名解除 Basic Auth

- [x] 用户明确授权移除用户名密码登录，并确认域名仅自行使用
- [x] 清理前备份 NPM 数据库、Proxy Host 9 配置和 Access List 1 文件：`/root/backups/model-verity/remove-basic-auth-20260729T162143Z`
- [x] Proxy Host 9 的 `access_list_id` 从 `1` 改为 `0`；Access List 本身保留，便于回滚
- [x] Nginx 配置测试通过；NPM 容器重启后无认证首页/API 均为 200，HTTP 仍 301 跳转 HTTPS
- [x] model-verity 服务保持 active，NPM SQLite integrity 为 `ok`

## 2026-07-25 — 移动端 Basic Auth 真实设备验收

- [x] 用户使用真实移动设备和移动专用凭据成功登录生产域名
- [x] 确认 HTTPS 直达入口、新 realm 与无歧义凭据解决 iOS Chrome 连续 401
- [x] 本项仅代表移动端登录验收通过；Review #003 的 References、Verify、History→Result 等整体 UI 门禁仍单独等待批准

## 2026-07-24 — 移动端 Basic Auth 登录修复

- [x] 从 NPM access log 定位真实失败会话为 iOS Chrome，18 次请求全部在 Nginx Basic Auth 层返回 401
- [x] 验证 htpasswd、数据库和原凭据一致；相同凭据以 iOS Safari/Android Chrome User-Agent 均返回 200
- [x] 稳定复现从 HTTP 跳转 HTTPS 时 Authorization 不跨 scheme 携带，要求使用 HTTPS 直达入口
- [x] 保留原账户并新增无 `0/O/1/I/l/i` 易混淆字符的移动账户；凭据仅写 root-only 文件
- [x] 轮换 Basic Auth realm，强制 iOS Chrome 丢弃旧失败凭据缓存
- [x] NPM 容器重启后两套账户、realm、HTTPS 首页/API 与 Authorization 清除均保持正常
- [x] 2026-07-25 用户在真实移动设备上确认可以正常登录

## 2026-07-24 — 分页视觉与供应商模型发现

- [x] 每页数量控件移至分页第二行中央，菜单按实际高度贴近 trigger，10/20/50 选项保持连续 44px 点击区
- [x] 供应商模型 ID 支持手动逗号分隔输入，也可从 Base URL 获取远程模型列表后搜索、选择并加入
- [x] OpenAI-compatible `/models` 与 Anthropic `/v1/models` adapter 接入；已保存 API Key 仅服务端解密使用，不返回浏览器
- [x] 地址或协议变化时禁止复用旧 key，要求重新输入；远端错误按实际 key 脱敏
- [x] 生产 9router 数据副本真实发现 GPT-5.6 / Claude Fable 5 等模型路由；明确目录 ID 不等同于真实性或内置指纹
- [x] 联网核验官方文档、OpenRouter 和 Zenodo：PAMELA 最新唯一数据版本无 GPT-5.6/Fable 5 采样，未伪造内置参考
- [x] 17 tests、typecheck、build、audit 和 320/390/768 Chromium 通过

## 2026-07-24 — 参考搜索、分页与供应商角色修复

- [x] Verify 的参考指纹 Select 增加可见搜索框，支持模型、标签、URL 和来源类型关键词
- [x] 下拉与 References 列表用暖橙“研究参考”和绿色“自建”明确区分来源
- [x] 内置研究参考列表改为分页，默认 10 条，支持上一页/下一页和每页 10/20/50 条
- [x] 修复审计供应商 eligibility 状态不一致：加载、保留选中和展示统一使用 `audit/either`
- [x] 确认生产唯一供应商角色为 `reference`，因此被审计下拉正确排除；部署时改为 `either` 以同时支持 Enrollment 和 Audit
- [x] 五档 Chromium 验证搜索、badge、分页、供应商过滤、菜单边界和移动横向溢出

## 2026-07-24 — 内置 PAMELA 研究指纹库与移动端加固

- [x] 审计 `pamela-publish-data` 与 Zenodo 数据/软件归档，确认数据为 OpenRouter 聚合路由研究采样，不冒充厂商第一方认证
- [x] 从 `main-02` 335,889 条归一化记录重新聚合整数 counts，不使用四舍五入分布
- [x] 按 40 cells 完整且每 cell `nValid >= 10` 纳入 146 个模型、排除 30 个
- [x] 生成可复现 `src/data/builtin-fingerprints.json.gz`，保存 counts/probs/有效性/协议/来源/校验摘要
- [x] 接入精确 `pamela@1.0.0` battery、语言 system prompts 和 `pamela@1` normalizer；保留本地 Enrollment `project-equivalent@v1`
- [x] Audit 按 reference 自动选择协议并禁止跨协议比较；内置 reference 只读且无需官方 API/Enrollment
- [x] SQLite 增加虚拟 reference 外部 ID 兼容，不破坏现有本地参考外键
- [x] UI 显示 OpenRouter 研究快照、DOI、CC BY 4.0 与非厂商认证；参考目录可搜索并分页显示（默认每页 10 项）
- [x] Select 菜单改为 body Portal，移动端底部面板、背景滚动锁、键盘/ARIA/前缀检索完整
- [x] 修复 Grid 最小内容宽度导致的 History/Result 顶栏拉伸和长 URL/model/cell 横向溢出
- [x] Chromium 验证 320×568、360×800、390×844、430×932、768×1024：0 几何失败
- [x] 最新 17 tests、typecheck、build、官方 registry npm audit 全部通过
- [x] npm README 改为包内自足；新增 `第三方声明.md`，构建时复制到白名单内的 `dist/data/第三方声明.md`

## 2026-07-24 — UI Review #002 Approved

- [x] 全部原生 `<select>/<option>` 替换为深色暖橙主题 Select
- [x] Select 支持点击外部关闭、完整键盘导航与 listbox ARIA 语义
- [x] 主按钮移除呼吸、流光、色带及所有装饰性循环动画
- [x] 主按钮回归静态暖橙底色，仅保留 hover、press 与 disabled 反馈
- [x] 用户在生产环境完成人工验收并批准 `verification-ui@v1.2`
- [x] TypeScript、13 项测试、生产构建与公网资源验证通过

---

## 2026-07-24 — 公网生产部署

- [x] 安装独立 Node.js 22 生产运行时与 `model-verity@0.1.0`
- [x] 创建非特权 `model-verity` 用户和 systemd 常驻服务
- [x] 服务仅绑定 NPM Docker bridge `172.19.0.1:8787`
- [x] NPM Proxy Host 9 从 Vite 5173 切换到生产 8787
- [x] 启用 HTTP→HTTPS、Basic Auth、Block Exploits 和 Authorization 清除
- [x] 重启 NPM 后配置持久化验证通过
- [x] 公网首页为生产静态构建，`/api/health` 返回 `model-verity 0.1.0`
- [x] 停止旧 Vite 5173 开发服务

---

## 2026-07-23 — npm 首次发布

- [x] 发布 `model-verity@0.1.0` 到官方 npm registry
- [x] `latest` dist-tag 指向 `0.1.0`
- [x] 官方 registry tarball 元数据验证通过
- [x] 从官方 registry 独立全局安装通过
- [x] 安装后的 `model-verity --version` 输出 `0.1.0`

---

## 2026-07-23 — Phase 3–7 统一对抗式检查

- [x] 按要求在全部可执行任务完成后才启动对抗检查
- [x] 覆盖阶段完整性、CLI/server、core、adapter、数据/密钥、API 安全、端到端和打包发布
- [x] 发现并修复 30 项功能、安全、兼容性和生命周期问题
- [x] 最终 13 tests、typecheck、production build、npm audit、npm pack/global install smoke 通过
- [x] 检查明细写入 `docs/对抗式检查.md`

---

## 2026-07-23 — Phase 7 v1 收尾（可执行项）

- [x] 接入真实 History 记录与运行详情
- [x] 接入并发、match/mid 阈值与 retainRaw 设置
- [x] 实现 Markdown、JSON、CSV 三种报告导出
- [x] 完成全量 typecheck、10 项测试和生产构建
- [x] 完成 `npm pack` 内容检查与临时 prefix 全局安装
- [x] 打包后的 CLI start/health/stop smoke 通过
- [x] 正式 npm publish 与公网生产反代记录为外部/人工事项

---

## 2026-07-23 — Phase 6 真实业务 UI 接线

- [x] 默认 Web 入口切换为真实 App，Preview Lab 保留于 `?preview=1`
- [x] 接入 Providers 添加、编辑、删除与 Key 掩码展示
- [x] 接入可信源 Enrollment 与参考指纹列表/删除
- [x] 接入 Audit setup、claimed identity、档位与参考选择
- [x] 接入真实进度轮询、取消、Result、JSD evidence 与可靠性指标
- [x] 完成 HTTP mock 8×30 Enrollment→8×15 Audit 端到端，结果 `likely_match`
- [x] UI 状态从 Approved 更新为 Logic wired

---

## 2026-07-23 — Phase 5 Adapters、SQLite、密钥与 API

- [x] 实现 adapter 类型、registry、OpenAI-compatible 与 Anthropic Messages
- [x] 实现 SQLite schema、WAL、迁移、Providers/References/Runs/Settings repository；Settings 现仅作历史兼容结构
- [x] 数据库只保存 secret reference，不保存 API Key 明文
- [x] 实现 keytar 动态优先路径与 AES-256-GCM 加密文件回退
- [x] 实现 Providers、References、Enrollment、Audit、Runs、Cancel、Settings API；Settings API 当前不由前端调用
- [x] 实现真实后台 RunManager：adapter→normalize→distribution→JSD→verdict
- [x] 实现请求体限制、URL/字段校验、secret header 拒绝与 API 脱敏
- [x] adapter/storage/API 测试与真实 HTTP smoke 通过
- [x] 当前主机 keytar 不可加载，降级状态记录于 `docs/待处理事项.md` OI-003

---

## 2026-07-23 — Phase 4 Core 协议引擎

- [x] 实现语义等价、版本化的 10 tasks × 4 languages = 40-cell battery
- [x] 实现 Unicode NFC、首答案提取、多语言数字/颜色/coin 归一化与有效性分类
- [x] 实现经验分布、base-2 JSD、cell 排除和平均分数
- [x] 实现 `likely_match` / `inconclusive` / `likely_mismatch` / `failed` 判定
- [x] 实现 Quick/Audit/Full/Enroll seeded cell 选择和样本乱序
- [x] 实现并发上限、429/5xx 重试、Retry-After、AbortSignal 与熔断
- [x] 添加 battery、normalize、JSD、planner、executor 和 mock 指纹测试，全部通过

---

## 2026-07-23 — Phase 3 CLI 与本地 server

- [x] 实现 `model-verity start [--host] [--port]` 前台服务
- [x] 实现 `stop`、`status [--json]`、`open`、help/version
- [x] 实现 runtime 文件、PID 探测、陈旧状态清理与信号退出清理
- [x] 实现 `GET /api/health`、Vite SPA 静态托管和 client route fallback
- [x] 默认绑定 `127.0.0.1`；公网绑定输出安全警告
- [x] 增加 Node TypeScript 配置与 tsup 构建
- [x] 修复构建产物重复 shebang 导致 CLI SyntaxError
- [x] 完成真实子进程启动、health、SPA、open、stop 集成验证

---

## 2026-07-23 — UI Review #001 Approved

- [x] 用户完成 Preview Lab 人工验收，P01–P12 全部通过
- [x] `docs/界面验收.md` 将 Review #001 记录为 **Approved**
- [x] UI 业务接线门禁解除；Phase 6 可在保持已批布局的前提下接线
- [x] 产品主题锁定为深色，删除浅色/System 自动切换
- [x] 背景锁定为 48px 淡暖橙方格纸，无粒子、光球、玻璃泡或线稿装饰
- [x] 顶栏与侧栏统一深色，侧栏使用中灰文字和暖橙选中态
- [x] Review #001 的初始按钮视觉完成验收；该按钮方案已由 Review #002 / `verification-ui@v1.2` 替代
- [x] `prefers-reduced-motion` 降级路径通过验收
- [x] 最新 `npm run typecheck` 与 `npm run build:ui` 通过

---

## 2026-07-22

### 产品与协议澄清（Grill-me）

- [x] 明确 v1 目标：**A 身份真实性 + D 可靠性**  
- [x] 对齐论文 arXiv:2607.10252 与 Zenodo 数据集关系  
- [x] 否定「仅配置 model 名 / 仅 logprobs 口号」的模糊做法  
- [x] 锁定验证主路径：Enrollment 参考指纹 + Audit 平均 JSD  
- [x] 锁定采样预算：默认 Audit 8×15；全 40 cell battery 内置抽 k  
- [x] 锁定参考策略：v1 仅用户 R1；公开库 v1.1  
- [x] 锁定主场景：非官方供应商审计  

### 工程决策

- [x] 包名 `model-verity`，目录 `/root/projects/model-verity`，单包 M1  
- [x] 技术栈 T2：CLI + Node API + React/Vite + SQLite  
- [x] CLI C2：start/stop/status/open，localhost，前台默认  
- [x] 协议 V1b+V1c：OpenAI-compatible + Anthropic，adapter 架构  
- [x] 结论 J3：绿/黄/红 + failed  
- [x] 安全 K4 + 样本 S3  
- [x] 并发 Q4 默认 4  

### UI 与门禁

- [x] Apple 风格验证三态 UI 规格（Setup / Running / Result）  
- [x] 三色可信度含义与视觉反馈规格  
- [x] **Preview Lab 门禁**流程：先预览验收再写业务接线  
- [x] 场景矩阵 P01–P12 与 《界面验收》模板  

### 仓库文档

- [x] 创建专属开发目录与 `docs/`  
- [x] `项目计划.md` `验证协议.md` `界面规范.md` `界面验收.md`  
- [x] `开发进度.md` `已完成事项.md` `后续路线图.md` `部署指南.md`  

---

## 2026-07-22（续）— Phase 1–2 工程落地

- [x] 初始化 npm 包 / Vite React / TypeScript  
- [x] Design tokens + app shell / Setup / Running / Result UI  
- [x] Preview Lab：`npm run preview:ui`，场景 P01–P12  
- [x] fixtures 与 View Model 类型对齐  
- [x] `typecheck` 与 `build:ui` 通过  
- [x] 界面验收 #001 人工 Approved（2026-07-23）  

---

## 当前未完成（勿误读为本表已做）

- 用独立数据证明预注册的 1%/1% 强结论错误目标
- 发布新的 npm 版本并从官方 registry 独立安装验证；需用户单独批准
